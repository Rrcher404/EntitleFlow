# EntitleFlow QA Report — 2026-03-26

**Run type:** Automated scheduled sweep
**Branch:** main
**Latest commit:** `439da28` — "fix: resolve 19 platform bugs from QA audit"

---

## Summary

| Check | Status | Details |
|---|---|---|
| TypeScript typecheck | **PASS** | 0 errors |
| ESLint | **FAIL** | 955 errors, 602 warnings |
| Production build (Vercel) | **PASS** | Latest deployment READY |
| Production build (local) | **SKIP** | Sandbox FUSE filesystem conflict — not a real issue |
| Deployment status | **PASS** | `dpl_F2JxLTQdH6TWnFRsYdkF3wCCYRZg` is READY on production |
| Database schema drift | **PASS** | 32/32 tables match between `database.types.ts` and live Supabase |
| Service role key exposure | **PASS** | No client components reference `SUPABASE_SERVICE_ROLE_KEY` |
| Organization scoping | **PASS** | All 11 company-admin routes filter by `organization_id` |
| Dark mode violations | **PASS** | 0 actual `dark:` Tailwind class usage (see notes) |

---

## Issues Found

### Critical (0)

No critical issues found.

### Warning (3)

**W1: 955 ESLint errors — primarily `@typescript-eslint/no-explicit-any`**
The vast majority of the 955 ESLint errors are `@typescript-eslint/no-explicit-any` violations across API routes and components. The lint configuration treats these as errors, but TypeScript itself compiles cleanly. Worst offenders by volume:

- `lib/notifications.ts` — 13 `any` errors
- `app/api/documents/[id]/auto-parse/route.ts` — 15 `any` errors
- `app/api/comments/[id]/resolve/route.ts` — 5 `any` errors
- `app/api/comments/[id]/route.ts` — 5 `any` errors
- `app/api/comments/bulk/route.ts` — 6 `any` errors

**W2: `@ts-nocheck` still present in 2 files**
- `app/api/email/inbound/route.ts` — has `@ts-nocheck` directive (ESLint flags as `@typescript-eslint/ban-ts-comment`)
- `app/api/email/send/route.ts` — has `@ts-nocheck` directive

**W3: `password_reset_tokens` reference in TODO comment**
- `app/api/admin/password-management/route.ts:25-26` contains a TODO comment referencing a `password_reset_tokens` table that doesn't exist. The code itself is safe (returns empty list), but the comment references a non-existent table.

### Info (4)

**I1: `app/api/leads/route.ts` has no auth check**
This is the only API route without authentication, but this is correct — it's a public lead capture endpoint for the marketing site.

**I2: Dark mode — JS object keys, not Tailwind classes**
`components/site/Logo.tsx` and `components/ui/onboarding-dialog.tsx` use `dark` as JavaScript object property names (for logo/color theming), not as Tailwind `dark:` class prefixes. This is architecturally fine — no actual dark mode Tailwind classes exist in the codebase.

**I3: 602 ESLint warnings — unused variables and missing deps**
Common patterns:
- Unused imports (Button, Input, icons imported but not used)
- React Hook `useEffect` missing dependencies in dependency arrays
- Unused destructured variables (`profile`, `organization`, `loading`)

**I4: Unescaped entities in JSX**
- `app/(auth)/login/page.tsx:184` — unescaped `'`
- `app/(auth)/signup/page.tsx:83` — unescaped `'`
- `app/app/dashboard/page.tsx:271` — unescaped `'`

---

## Fixes Applied

No autonomous fixes were applied in this sweep. All issues found are non-critical and require human judgment on approach (particularly the `any` type strategy — whether to fix incrementally or adjust ESLint severity).

---

## Deployment Status

| Deployment | State | Date | Commit |
|---|---|---|---|
| `dpl_F2JxLTQdH6TWnFRsYdkF3wCCYRZg` | **READY** | Latest | `439da28` — 19 platform bug fixes |
| `dpl_A1nRFDXGQv4dzy4skArfwMnUBB1J` | READY | Previous | `400b377` — Suspense boundaries |
| `dpl_2Dx8twp8qRPfbRwjyskcmwPKdbbi` | ERROR | Older | `7053779` — TS errors after removing ts-nocheck |
| `dpl_3RzN1WBGUTHDZYL2aLXuw8jtpL8s` | ERROR | Older | `7ee59de` — RBAC migration + type regen |

The most recent production deployment is healthy. The two ERROR deployments were mid-sprint fixes that were resolved in subsequent commits.

---

## Database Schema Alignment

All 32 tables in `lib/database.types.ts` match the live Supabase database exactly:

`activity_log`, `admin_audit_log`, `announcements`, `comment_assignments`, `comments`, `company_group_members`, `company_groups`, `deadlines`, `documents`, `email_queue`, `feature_flags`, `flowe_conversations`, `flowe_knowledge`, `flowe_messages`, `jurisdictions`, `license_definitions`, `marketing_leads`, `notification_preferences`, `notifications`, `organizations`, `parse_jobs`, `password_reset_config`, `permit_status_history`, `permits`, `platform_config`, `profiles`, `projects`, `role_permissions`, `team_invitations`, `team_members`, `user_activity_tracking`, `user_permission_overrides`

No drift detected.

---

## Metrics

| Metric | Count |
|---|---|
| `: any` occurrences (main codebase, excl. worktrees) | 33 |
| Files with >3 `any` instances | 2 (`settings/profile/page.tsx`: 7, `audit/export/route.ts`: 4) |
| `dark:` Tailwind violations | 0 |
| Unprotected API routes | 0 (leads route is intentionally public) |
| ESLint errors | 955 |
| ESLint warnings | 602 |
| `@ts-nocheck` files | 2 |
| Database schema drift | 0 tables |
| Unscoped company-admin queries | 0 / 11 routes |

---

## Recommendations

1. **Address `any` types incrementally.** The 33 occurrences in the main codebase are manageable. Priority targets: `lib/notifications.ts` (13 instances) and `app/api/documents/[id]/auto-parse/route.ts` (15 instances). Consider creating proper TypeScript interfaces for Supabase query results.

2. **Remove remaining `@ts-nocheck` directives.** `email/inbound/route.ts` and `email/send/route.ts` still suppress all type checking. These should be typed properly now that `database.types.ts` is up to date.

3. **Run `eslint --fix` for auto-fixable issues.** 12 errors and 4 warnings are auto-fixable (mostly `prefer-const` and unused eslint-disable directives).

4. **Clean up unused imports.** ~20+ files have unused imports contributing to the 602 warnings. A single pass with an auto-import cleanup tool would eliminate most of these.

5. **Fix unescaped JSX entities.** Three files have unescaped `'` characters — quick find-and-replace with `&apos;`.

6. **Production site verification.** WebFetch was blocked by egress proxy. Manual verification of `entitleflow.com` is recommended to confirm the latest deployment renders correctly.

---

*Report generated automatically by EntitleFlow QA scheduled task.*
