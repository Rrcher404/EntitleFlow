# EntitleFlow QA Report — 2026-03-28

**Run type:** Automated scheduled sweep
**Model:** Claude Opus 4.6
**Branch:** main
**Latest commit:** `bc17496` — "fix: remove self-service signup, redirect /signup to /walkthrough"

---

## Summary

| Check | Status |
|---|---|
| TypeScript typecheck | **PASS** — zero errors |
| ESLint | **FAIL** — 226 errors, 129 warnings |
| Production build (Vercel) | **PASS** — latest deployment READY |
| Production build (sandbox) | **SKIP** — EPERM due to sandbox FUSE filesystem lock on `.next/` |
| Deployment status | **PASS** — `dpl_64ZAt6rts8fsvGWymvULWcA8vKCm` is READY (production) |
| Production site accessible | **PASS** — entitleflow.com returns full HTML |
| Dark mode violations | **PASS** — 0 Tailwind `dark:` classes found |
| Service role key exposure | **PASS** — key only referenced in server-side files |
| Auth on API routes | **PASS** — 60/61 routes authenticated; 1 intentionally public (`/api/leads`) |
| Incorrect table references | **PASS** — no references to known-wrong table names |
| Database schema drift | **WARNING** — 3 tables in DB not in `database.types.ts` |

---

## Issues Found

### Critical

*None.*

### Warning

#### 1. ESLint: 226 errors across the codebase
The vast majority (180+) are `@typescript-eslint/no-explicit-any` violations. While TypeScript compilation passes clean, the widespread use of `any` degrades type safety. Key hotspots:

| File | `any` count |
|---|---|
| `app/app/settings/profile/page.tsx` | 7 |
| `app/api/company-admin/audit/export/route.ts` | 4 |
| `app/api/company-admin/users/[id]/route.ts` | 3 |
| `app/api/company-admin/storage/route.ts` | 3 |
| `lib/notifications.ts` | 13 |
| `lib/ai/embeddings.ts` | 8 |
| `app/api/documents/[id]/auto-parse/route.ts` | 15 |
| `app/api/comments/[id]/resolve/route.ts` | 5 |
| `app/api/comments/[id]/route.ts` | 5 |
| `app/api/comments/bulk/route.ts` | 6 |

Other notable ESLint errors:
- **`react-hooks/static-components`** in `app/demo/projects/page.tsx`: `SortArrow` component defined inside render function (lines 300-303). Will cause unnecessary re-renders.
- **`react-hooks/immutability`** in `components/app/notification-toast.tsx`: Direct `window.location.href` assignment inside event handler (line 111). Should use `router.push()`.
- **`react-hooks/set-state-in-effect`** in `components/ui/onboarding-dialog.tsx`: `setIsOpen(true)` called synchronously in useEffect (line 184). Should initialize state from localStorage in a lazy initializer.
- **`@typescript-eslint/no-empty-object-type`** in `components/ui/card.tsx`: 6 empty interface declarations.
- **`react/no-unescaped-entities`** in pricing page and notification-bell component.
- **`prefer-const`** in 2 files.

#### 2. Database schema drift — 3 tables missing from types
Tables present in live Supabase but absent from `lib/database.types.ts`:
- `license_change_requests`
- `organization_contracts`
- `contract_change_usage`

These were added in migration `00008` but types were never regenerated. Code referencing these tables must use type casts or workarounds, reducing type safety.

**In types but not tables (expected):**
- `check_user_permission` — database function, not a table
- `is_org_admin` — database function, not a table
- `match_flowe_knowledge` — database function, not a table

#### 3. 42 unused variable/import warnings
Scattered across admin pages, marketing components, and lib files. Not blocking but adds noise. Key files:
- `app/admin/leads/page.tsx` (3 unused imports)
- `app/admin/organizations/[id]/page.tsx` (4 unused imports)
- `app/admin/users/page.tsx` (3 unused vars)

### Info

#### 4. Dark mode scan — false positives only
The `dark:` keyword appears in 2 main-codebase files but as **JS object property names**, not Tailwind classes:
- `components/site/Logo.tsx:27` — `dark: { ... }` color variant object
- `components/ui/onboarding-dialog.tsx:13-37` — `dark: string` and `colors: { dark: '#...' }` config objects

No actual Tailwind `dark:` utility classes exist. **Compliant with light-mode-only policy.**

#### 5. Service role key — properly scoped
`SUPABASE_SERVICE_ROLE_KEY` is referenced only in:
- `lib/supabase/server.ts:36` (server utility)
- `lib/admin/auth.ts:22` (server auth helper)
- `app/api/email/inbound/route.ts:79` (error message string)

None of these files contain `'use client'`. **No security violation.**

#### 6. Public API route: `/api/leads`
This route accepts POST requests without authentication. This is intentional — it handles marketing lead form submissions from the public website. The route validates input against `marketingLeadSchema` (Zod). Consider adding rate limiting if not already present at the Vercel/infrastructure level.

---

## Fixes Applied

*No autonomous fixes were applied in this run.* The ESLint errors are widespread and interconnected (primarily `any` type usage across 30+ files), making targeted auto-fixes risky without a coordinated effort. The build failure in the sandbox is environment-specific and does not affect Vercel deployments.

---

## Recommendations

### High Priority
1. **Regenerate database types.** Run `npx supabase gen types typescript` to bring `lib/database.types.ts` in sync with migration 00008 tables (`license_change_requests`, `organization_contracts`, `contract_change_usage`). This will improve type safety across the license management routes.

2. **Fix the 3 React hook rule violations.** These are not just lint noise — they cause real bugs:
   - Move `SortArrow` outside the component in `demo/projects/page.tsx`
   - Replace `window.location.href = ...` with `router.push()` in `notification-toast.tsx`
   - Use lazy state initializer instead of `setIsOpen` in `useEffect` in `onboarding-dialog.tsx`

### Medium Priority
3. **Reduce `any` types systematically.** Start with the highest-impact files: `lib/notifications.ts` (13 occurrences), `app/api/documents/[id]/auto-parse/route.ts` (15), `app/api/comments/bulk/route.ts` (6). Use the Supabase-generated types from `database.types.ts` as the source of truth for API response shapes.

4. **Clean up unused imports.** Run `npm run lint -- --fix` to auto-fix the 3 fixable issues, then manually remove the ~42 unused imports/variables. This reduces bundle size slightly and improves code readability.

### Low Priority
5. **Fix empty interfaces in `card.tsx`.** Replace the 6 empty interfaces with type aliases: `type CardHeaderProps = React.HTMLAttributes<HTMLDivElement>`.

6. **Add rate limiting to `/api/leads`.** While the route is intentionally public, it should have request-rate protection to prevent abuse.

7. **Clean up `.eslintignore` deprecation.** Migrate ignore patterns from `.eslintignore` to the `ignores` property in `eslint.config.js`.

---

## Metrics

| Metric | Value |
|---|---|
| TypeScript errors | 0 |
| ESLint errors | 226 |
| ESLint warnings | 129 |
| `any` type occurrences (main codebase) | ~108 across ~30 files |
| Dark mode (`dark:`) Tailwind violations | 0 |
| Unprotected API routes | 1 (intentional: `/api/leads`) |
| Service role key in client code | 0 |
| Incorrect table references | 0 |
| DB tables missing from types | 3 (`license_change_requests`, `organization_contracts`, `contract_change_usage`) |
| Types referencing DB functions (not tables) | 3 (expected) |
| Latest deployment state | READY |
| Total Vercel deployments reviewed | 20 (8 READY production, 5 ERROR historic, 2 READY preview) |

---

## Deployment History (Last 7 Days)

| Deployment | Date | Status | Commit |
|---|---|---|---|
| `dpl_64ZAt6r...` | Mar 28 | READY | Remove self-service signup |
| `dpl_Dbbvnf9...` | Mar 28 | READY | Wire email notifications (redeploy) |
| `dpl_D4hP7Tw...` | Mar 28 | READY | Wire email notifications |
| `dpl_QDFKnXD...` | Mar 27 | READY | License change request pipeline |
| `dpl_GqUJCpn...` | Mar 27 | READY | FlowE pricing, seat enforcement |
| `dpl_2Y4mJUJ...` | Mar 27 | READY | Admin analytics fixes |
| `dpl_9uSSePt...` | Mar 27 | READY | Stabilization sprint — 12 bug fixes |
| `dpl_F2JxLTQ...` | Mar 26 | READY | Resolve 19 platform bugs |

All recent deployments are READY. The last ERROR deployments were on Mar 21-22 and were fixed in subsequent commits.

---

*Report generated automatically by EntitleFlow Deep Test scheduled task.*
