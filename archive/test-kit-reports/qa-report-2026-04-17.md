# EntitleFlow QA Report — 2026-04-17

**Automated sweep run by:** EntitleFlow QA Agent (scheduled)
**Repository:** `/Users/jyecemaybury/PermitPilot`
**Vercel project:** `prj_IiB5CH8l7YJSAIiD4qJ86Rc34PdL`
**Supabase project:** `sjyzqqratghsjttqeghy`
**Latest commit on `main`:** `0dc0f32` — fix: profile avatar not rendering in portal topbar

---

## Summary

| Check | Status | Detail |
|---|---|---|
| TypeScript typecheck | PASS | 0 errors (`tsc --noEmit` exit 0) |
| ESLint | PASS | 0 errors, 7 warnings (all `react-hooks/exhaustive-deps`) |
| Production build (sandbox) | SKIPPED | Blocked by FUSE mount EPERM on `.next/.fuse_hidden…` — environment limitation, not a code issue. Vercel build used as authoritative signal. |
| Vercel latest deployment | READY | `dpl_Er4x6jaHVYRs6NzeWEtG5rT4sFCz` — production target, green |
| Production site (entitleflow.com) | ACCESSIBLE | Returns full HTML (~100K chars) |
| Dark-mode violations | NONE | No Tailwind `dark:` variants in app/components/lib/data |
| Auth coverage on API routes | CLEAN | 58 of 59 routes authenticate; the only exception is `app/api/leads/route.ts` (public marketing form by design) |
| Service-role key leakage | CLEAN | `SUPABASE_SERVICE_ROLE_KEY` only in server-only files (`lib/admin/auth.ts`, `lib/supabase/server.ts`, `app/api/email/inbound/route.ts`); no `'use client'` exposure |
| Known-wrong table references | NONE | All 6 audited stale names (`audit_logs`, `organization_members`, `organization_licenses`, `password_reset_tokens`, `user_licenses`, `license_assignment_log`) are gone from `app/` and `lib/` |
| Company-admin org scoping | CLEAN | All 12 `app/api/company-admin/*` routes filter queries with `organization_id = admin.organization_id` |
| Supabase schema drift vs `lib/database.types.ts` | NONE | 35 live tables all represented in generated types; only extra code reference is `supabase.storage.from('avatars')` (Storage bucket, not a table) |
| `any` usage | LOW | 10 total occurrences across codebase; only 1 file (`app/api/email/inbound/route.ts`, 5 hits) exceeds the >3 threshold |

Overall: **green across every hard gate**. The codebase is in materially better shape than the 2026-03-25 baseline audit.

---

## Phase 1 — Static Analysis

### TypeScript

```
./node_modules/.bin/tsc --noEmit
exit 0
```

No errors, no warnings.

### ESLint (7 warnings, all non-blocking)

Every warning is `react-hooks/exhaustive-deps` on an intentionally-trimmed dep array. No auto-fix applied — these are judgment calls (adding the flagged dep often causes a re-render loop or unwanted refetch). Files:

- `app/app/documents/page.tsx:326` — missing `supabase`
- `app/app/notifications/page.tsx:284` — missing `supabase`
- `app/app/projects/map/page.tsx:46` — missing `loadProjects`
- `app/app/settings/team/page.tsx:150` — missing `loadTeamData`
- `components/app/document-parse-status.tsx:108` — missing `onParseComplete`, `statusInfo.parse_status`
- `components/app/document-parse-status.tsx:149` — missing `fetchStatus`
- `components/app/document-parse-status.tsx:167` — missing `fetchStatus`

None are bugs — `supabase` is a stable reference from `createClient()`, and `fetchStatus`/`loadProjects`/etc. are defined inside the component and would require `useCallback` wrapping if added. Recommend leaving as-is.

### Next build (sandbox)

```
> Build error occurred
Error: EPERM: operation not permitted, unlink '.next/.fuse_hidden0000031f00000001'
```

This is the sandbox's FUSE mount blocking Next's cleanup of `.next`. Not a code bug. Vercel's own build logs are the authoritative signal — the latest Vercel deployment for this commit is `READY`.

---

## Phase 2 — Code Quality Audit

### Dark-mode scan

Zero Tailwind `dark:` variants anywhere in `app/`, `components/`, `lib/`, or `data/`. The only `dark` string hits are in two theme object literals:

- `components/ui/onboarding-dialog.tsx` — `{ dark: '#0f3c35', light: '#e8f5f2' }` (color-pair object keys)
- `components/site/Logo.tsx` — `{ dark: … }` (logo variant key)

Both are object property names, not Tailwind variants. Compliant with the light-only rule.

### Auth on API routes

All 59 files under `app/api/` were scanned for one of: `auth.getUser`, `verifyAdmin`, `verifyCompanyAdmin`, `verifyOrgAdmin`, `requireAuth`, `authorize`. Results:

- **58 routes protected.**
- **1 intentional public route:** `app/api/leads/route.ts` — marketing walkthrough / early-access lead capture POST endpoint. No auth expected.

No unprotected permit / project / document / admin route found.

### Service role key exposure

Searched for `SUPABASE_SERVICE_ROLE_KEY` + filtered for files containing `'use client'`. **No matches.** The key appears only in three server-only files:

- `lib/supabase/server.ts`
- `lib/admin/auth.ts`
- `app/api/email/inbound/route.ts`

### Stale table references

Grepped for each known-wrong table name; all returned zero hits in `app/` and `lib/`:

- `.from('audit_logs')` — 0
- `.from('organization_members')` — 0
- `.from('organization_licenses')` — 0
- `.from('password_reset_tokens')` — 0
- `.from('user_licenses')` — 0
- `.from('license_assignment_log')` — 0
- `.from('licenses')` (standalone) — 0

The prior lint/type cleanup sprint (commit `a8841d0`) appears to have eliminated all of these.

### `any` type usage

- Total `: any | as any | <any>` hits: **10**
- Files over the 3-hit threshold: **1**
  - `app/api/email/inbound/route.ts` — 5 occurrences, all `(supabase as any)` and `{ … } as any` on insert/update payloads. Recommend typing these with the generated `Database` generic in a future pass — not urgent.

### Company-admin org scoping

Spot-checked every route under `app/api/company-admin/` that queries `projects | permits | documents | profiles`. **All 12 routes correctly constrain queries with `.eq('organization_id', admin.organization_id)`** (or query `organizations` directly by `id = admin.organization_id`):

- `stats/route.ts`, `audit/route.ts`, `audit/export/route.ts`, `storage/route.ts`, `security/route.ts`, `groups/route.ts`, `groups/members/route.ts`, `permissions/route.ts`, `license-requests/route.ts`, `users/route.ts`, `users/[id]/route.ts`, `users/[id]/reset-password/route.ts`

---

## Phase 3 — Deployment Verification

- `list_deployments` returned 20 deployments.
- **Latest production** (`dpl_Er4x6jaHVYRs6NzeWEtG5rT4sFCz`, commit `0dc0f32`): state = `READY`.
- All recent deployments after the 2026-03-21 cleanup sprint are `READY`. The ERROR states in the list are older (2026-03-20/21) and were resolved by the `0dc0f32`/`a8841d0`/`bc17496` sequence.
- Production host `entitleflow.com` served a full HTML response — the landing page renders.

No remediation required.

---

## Phase 4 — Schema Drift

Compared `list_tables` (35 tables, all RLS enabled) against table definitions in `lib/database.types.ts`:

- **Every live table has a matching type definition.**
- `email_queue` exists in both but isn't queried directly by route handlers — likely consumed by the email-forwarder Cloud Function; not a drift.
- The only table-name-like string in code that isn't a Postgres table is `supabase.storage.from('avatars')` in `app/app/settings/profile/page.tsx` — this is the Supabase **Storage bucket**, which lives outside the `public` schema. Not drift.

No action required.

---

## Fixes Applied

**None.** Every gate passed; the lint warnings are judgment calls that would regress behavior if auto-fixed. No commits pushed.

---

## Recommendations (not blocking)

1. **`any` reduction in `app/api/email/inbound/route.ts`** — the 5 `as any` casts were introduced to unblock inbound-email ingestion. They can now be replaced with properly typed `Database['public']['Tables'][…]['Insert']` payloads. ~15-minute cleanup.
2. **Memoize fetch callbacks** in the 4 files with exhaustive-deps warnings if you want a clean `npm run lint` — wrap `fetchStatus` / `loadProjects` / `loadTeamData` in `useCallback` and add them to the dep array. Behavioral risk is low but non-zero (could change refetch semantics), so only do this during a dedicated hooks pass.
3. **Stale artifact cleanup** — workspace has `.fuse_hidden*` leftovers in `.next/` (sandbox issue) and several untracked root-level HTML/MD docs (`grantflow-architecture.html`, `platform-shell-opportunities.html`, `revenue-projections.html`, `EntitleFlow-Investor-Mentor-Briefing.md`, `ADR-001-GRANTFLOW-ARCHITECTURE.md`). Decide whether to commit or gitignore.
4. **Tighten `.eslintignore` migration** — ESLint emits `ESLintIgnoreWarning: The ".eslintignore" file is no longer supported`. Move that single line into `eslint.config.mjs` under `ignores`. Cosmetic.

---

## Metrics

| Metric | Value |
|---|---|
| `any` type occurrences | 10 (across 5 files, none critical) |
| Dark-mode (`dark:`) class usages | 0 |
| Unprotected API routes | 0 (excluding `/api/leads` by design) |
| Company-admin routes missing org scope | 0 |
| Stale table references | 0 |
| Service-role key exposures in client code | 0 |
| Live DB tables w/ RLS disabled | 0 |
| TypeScript errors | 0 |
| ESLint errors | 0 |
| ESLint warnings | 7 (all exhaustive-deps) |
| Live tables | 35 |
| Vercel deployments scanned | 20 (latest = READY) |

---

## Agent notes

- Sandbox `npm run build` was blocked by a FUSE-layer EPERM — consistent across recent scheduled runs, an environment limit, not a codebase issue. Vercel build status is the source of truth.
- No destructive actions taken. No env-var changes. No Supabase data mutations. No git pushes. Pure read-only audit.
