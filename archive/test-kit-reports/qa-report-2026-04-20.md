# EntitleFlow QA Sweep — 2026-04-20

Automated QA and code-health run. Triggered by scheduled task; executed against `main` at commit `0dc0f32` (fix: profile avatar not rendering in portal topbar).

## Summary

| Check | Result |
|---|---|
| TypeScript `tsc --noEmit` | PASS (0 errors) |
| ESLint `npm run lint` | PASS (0 errors, 7 warnings) |
| Production `npm run build` | PASS (111 static pages, 60s compile, 91s total) |
| Vercel latest deployment | READY (`dpl_Er4x6jaHVYRs6NzeWEtG5rT4sFCz`) |
| entitleflow.com reachable | YES (HTML served, ~100KB) |
| Supabase schema drift | NONE (35 tables on both sides, exact match) |

Status: **all green**. No auto-fixes required. No commits were made during this run.

## Issues Found

### Critical
_None._

### Warning

**Lint — 7 `react-hooks/exhaustive-deps` warnings.** All are in client components where a callback or effect intentionally omits a dependency. These are pre-existing and known (surfaced in the April 18 cleanup that took lint from 226 errors → 0). Listed here for completeness:

- `app/app/documents/page.tsx:326` — useCallback missing `supabase`
- `app/app/notifications/page.tsx:284` — useCallback missing `supabase`
- `app/app/projects/map/page.tsx:46` — useEffect missing `loadProjects`
- `app/app/settings/team/page.tsx:150` — useEffect missing `loadTeamData`
- `components/app/document-parse-status.tsx:108` — useCallback missing `onParseComplete`, `statusInfo.parse_status`
- `components/app/document-parse-status.tsx:149` — useEffect missing `fetchStatus`
- `components/app/document-parse-status.tsx:167` — useEffect missing `fetchStatus`

**Build — Next.js middleware deprecation warning.** `next build` prints:

> The "middleware" file convention is deprecated. Please use "proxy" instead.

Next.js 16 is renaming `middleware.ts` → `proxy.ts`. Non-blocking today, but it will be a required migration in a later minor. No action taken.

**Lint — `.eslintignore` deprecation warning.** ESLint now prefers the `ignores` key inside `eslint.config.js`. The current `.eslintignore` still works but prints a warning each run.

### Info

**Uncommitted files in the working tree.** Several `test-kit/qa-report-YYYY-MM-DD.md` files from prior runs, plus `ADR-001-GRANTFLOW-ARCHITECTURE.md`, three standalone HTML artifacts, the investor briefing markdown, and a modified `next-env.d.ts`. Not blocking, but they have accumulated. Consider a `.gitignore` entry for `test-kit/qa-report-*.md` or a single bulk commit.

## Phase-by-Phase Findings

### Phase 1 — Static Analysis

- **Typecheck:** ran `./node_modules/.bin/tsc --noEmit` in ~6s. Exit 0, no output. Clean.
- **Lint:** ran `npm run lint`. Exit 0. 0 errors, 7 warnings (all `react-hooks/exhaustive-deps`).
- **Build:** ran `npm run build`. Exit 0. Turbopack compile 60s, 9-worker page generation of 111 pages in 2.6s, total runtime 91.69s. No failed routes, no missing imports, no Suspense boundary complaints.

Every API route in `app/api/` was compiled as a dynamic `ƒ` route as expected. All 111 marketing/app/demo pages either prerendered static (`○`) or SSG'd via `generateStaticParams` (`●`).

### Phase 2 — Code Quality Audit

- **Dark-mode violations:** zero Tailwind `dark:` utility classes in source. The only occurrences of the literal string `dark:` are (a) object property names like `dark: string` in typed color tuples (`components/site/Logo.tsx`, `components/ui/onboarding-dialog.tsx`), and (b) a single mention in `components/marketing/README.md` documenting that dark-mode utilities are *not* used. No fixes needed.

- **API route auth:** scanned every file in `app/api/**/route.ts` (60 routes) for at least one of `getUser()`, `verifyCompanyAdmin`, `verifyAdmin`, `requireAuth`, webhook-signature check, or `CRON_SECRET` gate. The only route with none of these is `app/api/leads/route.ts` — which is the **public** marketing lead-capture endpoint and is correct as written (validates via Zod, creates a marketing_leads row). Not a violation.

- **Service-role key leak:** `SUPABASE_SERVICE_ROLE_KEY` appears only in `lib/supabase/server.ts`, `lib/admin/auth.ts`, and `app/api/email/inbound/route.ts` (all server-only). Zero occurrences in any `'use client'` file. Clean.

- **Known-bad table references** (`audit_logs`, `organization_members`, `organization_licenses`, `password_reset_tokens`, `licenses`, `user_licenses`, `license_assignment_log`): **zero hits** across the codebase. The March cleanup that migrated to `admin_audit_log`, `team_members`, `license_definitions`, `password_reset_config` held.

- **`any` type usage:** **zero** occurrences of `: any` across `app/`, `lib/`, and `components/`. This matches the April 18 cleanup commit message ("Phase 2: Remove 196 vestigial as any casts").

- **Organization scoping on company-admin routes:** every `app/api/company-admin/**/route.ts` file that queries `projects`, `permits`, `documents`, or `profiles` includes `organization_id` filtering. No missing-scope leaks found.

### Phase 3 — Deployment Verification

Latest deployment `dpl_Er4x6jaHVYRs6NzeWEtG5rT4sFCz` is **READY** in production, created from commit `0dc0f32` on `main`. Previous 3 deployments also READY; the last ERROR deployments are all from March 20–21 during the RBAC migration sprint and have been superseded.

`entitleflow.com` returned a full HTML response (≈100KB, marketing homepage). No action needed.

### Phase 4 — Supabase Schema Drift

Live Supabase (`sjyzqqratghsjttqeghy`, schema `public`): **35 tables**.
`lib/database.types.ts`: **35 tables**.

Set-equality verified — the two sides are identical:

```
activity_log, admin_audit_log, announcements, comment_assignments, comments,
company_group_members, company_groups, contract_change_usage, deadlines,
documents, email_queue, feature_flags, flowe_conversations, flowe_knowledge,
flowe_messages, jurisdictions, license_change_requests, license_definitions,
marketing_leads, notification_preferences, notifications, organization_contracts,
organizations, parse_jobs, password_reset_config, permit_status_history, permits,
platform_config, profiles, projects, role_permissions, team_invitations,
team_members, user_activity_tracking, user_permission_overrides
```

All 35 tables have RLS enabled.

## Fixes Applied

None. Codebase passed every check cleanly; no auto-fix was required or appropriate.

## Recommendations (Human Attention)

1. **Resolve 7 exhaustive-deps warnings.** Low priority but trivially fixable. In each case either add the missing dep to the array, or wrap the effect in `useCallback`/`useMemo` properly. These tend to mask real stale-closure bugs later. Touch `app/app/documents/page.tsx`, `app/app/notifications/page.tsx`, `app/app/projects/map/page.tsx`, `app/app/settings/team/page.tsx`, and `components/app/document-parse-status.tsx`.

2. **Plan the `middleware → proxy` rename.** Next.js 16 is deprecating the `middleware.ts` convention in favor of `proxy.ts`. The current file at `middleware.ts` will keep working for now, but scheduling a low-risk PR to rename + update any docs would remove a recurring build warning and front-run the eventual forced migration.

3. **Migrate `.eslintignore` → `ignores` in `eslint.config.js`.** One-line change in config, kills another recurring warning line in CI output.

4. **Consider gitignoring `test-kit/qa-report-*.md`.** These accumulate daily and clutter `git status`. If they're intended to be committed, a single `chore: archive qa reports` bulk commit would clean up 15+ untracked files in one pass.

5. **No action on the two ERROR deployments from March 21** (`dpl_2Dx8twp8qRPfbRwjyskcmwPKdbbi`, `dpl_33JNdm24YxGGh7t2WvsBrvWtKZqg`, `dpl_3RzN1WBGUTHDZYL2aLXuw8jtpL8s`, `dpl_8s9ZpemjfuXBc5bQmS8y7iWQWkz6`, `dpl_3Syp9HuPjLUatk54okpoujpbiGbk`). All were part of the RBAC migration sprint and have been superseded by the subsequent READY deployments. Historical only.

## Metrics

| Metric | Count |
|---|---|
| `: any` occurrences in source | 0 |
| Tailwind `dark:` utility classes | 0 |
| Unprotected API routes (touching permit/project/document data) | 0 |
| Service-role-key leaks in client code | 0 |
| API routes total (`app/api/**/route.ts`) | 60 |
| TypeScript errors | 0 |
| ESLint errors | 0 |
| ESLint warnings | 7 |
| Known-bad table name references | 0 |
| Schema drift (tables) | 0 |
| Build time (Turbopack) | 91.69s |
| Static pages generated | 111 |
| Tables in live Supabase (public schema) | 35 |
| Tables in `lib/database.types.ts` | 35 |

---

**Report generated:** 2026-04-20 by scheduled QA task `entitleflow-deep-test`
**Commit scanned:** `0dc0f32` on `main`
**Production deployment:** `dpl_Er4x6jaHVYRs6NzeWEtG5rT4sFCz` (READY)
