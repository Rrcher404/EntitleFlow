# EntitleFlow QA Report — 2026-04-03

**Run type:** Automated scheduled task (Cowork)
**Commit at time of scan:** `0dc0f3262b6ea670f816a814643abf4e7ad9e2e1` (main)

---

## Summary

| Check | Status | Details |
|---|---|---|
| TypeScript typecheck | **PASS** | 0 errors |
| ESLint | **PASS** | 0 errors, 7 warnings (all `react-hooks/exhaustive-deps`) |
| Production build (Vercel) | **PASS** | Latest deployment READY (sandbox build N/A — FUSE filesystem) |
| Dark mode violations | **PASS** | 0 Tailwind `dark:` classes found |
| Service role key exposure | **PASS** | Only in server-side files |
| Auth on API routes | **PASS** | All company-admin routes use `verifyCompanyAdmin()` |
| Organization scoping | **PASS** | All company-admin queries filter by `organization_id` |
| Incorrect table references | **PASS** | No phantom table names in `app/api/` or `lib/` |
| `any` type usage | **PASS** | 0 occurrences in main codebase |
| Database schema drift | **PASS** | 35/35 tables match between `database.types.ts` and live Supabase |
| Deployment status | **PASS** | Latest production deployment READY |

**Overall: ALL CHECKS PASS**

---

## Phase 1: Static Analysis

### TypeScript Typecheck
```
./node_modules/.bin/tsc --noEmit → 0 errors
```

### ESLint
```
0 errors, 7 warnings
```

All 7 warnings are `react-hooks/exhaustive-deps`:

| File | Hook | Missing Dependency |
|---|---|---|
| `app/app/documents/page.tsx:326` | useCallback | `supabase` |
| `app/app/notifications/page.tsx:284` | useCallback | `supabase` |
| `app/app/projects/map/page.tsx:46` | useEffect | `loadProjects` |
| `app/app/settings/team/page.tsx:150` | useEffect | `loadTeamData` |
| `components/app/document-parse-status.tsx:108` | useCallback | `onParseComplete`, `statusInfo.parse_status` |
| `components/app/document-parse-status.tsx:149` | useEffect | `fetchStatus` |
| `components/app/document-parse-status.tsx:167` | useEffect | `fetchStatus` |

These are intentional omissions to prevent re-fetch loops. Severity: **info**.

### Production Build
The sandbox cannot run `next build` due to FUSE filesystem restrictions (`.next/.fuse_hidden*` files cannot be unlinked). However, the latest Vercel deployment built successfully, confirming the production build passes.

---

## Phase 2: Code Quality Audit

### Dark Mode Violations
**Result: PASS** — Searched for `dark:\w` pattern across all `.tsx`, `.ts`, `.jsx`, `.css` files. Zero Tailwind `dark:` variant classes found. The `dark:` string appears only as JavaScript object property names in `Logo.tsx` and `onboarding-dialog.tsx` (color palette definitions), which is correct usage.

### Service Role Key Exposure
**Result: PASS** — `SUPABASE_SERVICE_ROLE_KEY` appears in code only in:
- `lib/supabase/server.ts` (server-only)
- `lib/admin/auth.ts` (server-only)

No files with `'use client'` directive reference this key. No API route files contain `'use client'`.

### Auth on API Routes (Company Admin)
**Result: PASS** — All 12 route files under `app/api/company-admin/` call `verifyCompanyAdmin()` as the first operation in every handler (GET, POST, PATCH, DELETE). No unprotected endpoints.

| Route | Methods | Auth | Org Scoping |
|---|---|---|---|
| `security/` | GET, PATCH | verifyCompanyAdmin | password_reset_config filtered |
| `groups/` | GET, POST, PATCH, DELETE | verifyCompanyAdmin | company_groups filtered |
| `groups/members/` | POST, DELETE | verifyCompanyAdmin | groups + profiles filtered |
| `permissions/` | GET, POST | verifyCompanyAdmin | overrides filtered |
| `storage/` | GET | verifyCompanyAdmin | organizations + projects filtered |
| `license-requests/` | GET, DELETE | verifyCompanyAdmin | requests filtered |
| `audit/` | GET | verifyCompanyAdmin | audit_log filtered |
| `audit/export/` | GET | verifyCompanyAdmin | audit + activity filtered |
| `users/` | GET, PATCH | verifyCompanyAdmin | profiles filtered |
| `users/[id]/` | GET, PATCH | verifyCompanyAdmin | profiles filtered |
| `users/[id]/reset-password/` | POST | verifyCompanyAdmin | profile verified |
| `stats/` | GET | verifyCompanyAdmin | all tables filtered |

### Incorrect Table References
**Result: PASS** — Searched for `audit_logs`, `organization_members`, `organization_licenses`, `password_reset_tokens`, `licenses` (standalone), `user_licenses`, `license_assignment_log` in `app/api/` and `lib/`. Zero matches.

### `any` Type Usage
**Result: PASS** — Zero `: any` occurrences in the main codebase (`app/`, `lib/`, `components/`). All 69 matches found in the prior scan were exclusively in `.claude/worktrees/` directories (stale worktree copies).

---

## Phase 3: Deployment Verification

### Latest Deployment
| Field | Value |
|---|---|
| ID | `dpl_Er4x6jaHVYRs6NzeWEtG5rT4sFCz` |
| State | **READY** |
| Target | production |
| URL | `entitle-flow-j0pi6arsm-entitleflow-nc.vercel.app` |
| Commit | `0dc0f32` — "fix: profile avatar not rendering in portal topbar" |
| Created | 2026-04-03 |

The last 10 production deployments are all READY. No active deployment failures.

### Deployment History (recent)
- 5 older ERROR deployments exist (from 2026-03-21/22 sprint) — all superseded by successful subsequent deploys.

---

## Phase 4: Database Schema Drift

**Result: PASS** — Perfect alignment between `lib/database.types.ts` and live Supabase.

**35 tables in both:**

activity_log, admin_audit_log, announcements, comment_assignments, comments, company_group_members, company_groups, contract_change_usage, deadlines, documents, email_queue, feature_flags, flowe_conversations, flowe_knowledge, flowe_messages, jurisdictions, license_change_requests, license_definitions, marketing_leads, notification_preferences, notifications, organization_contracts, organizations, parse_jobs, password_reset_config, permit_status_history, permits, platform_config, profiles, projects, role_permissions, team_invitations, team_members, user_activity_tracking, user_permission_overrides

All tables have RLS enabled.

---

## Fixes Applied

None required — all checks passed.

---

## Metrics

| Metric | Count |
|---|---|
| `any` types (main codebase) | **0** |
| Dark mode violations | **0** |
| Unprotected API routes | **0** |
| ESLint errors | **0** |
| ESLint warnings | **7** |
| TypeScript errors | **0** |
| Tables in schema | **35** |
| Schema drift mismatches | **0** |
| Deployment state | **READY** |

---

## Recommendations

1. **ESLint warnings (info):** The 7 `react-hooks/exhaustive-deps` warnings in `documents/page.tsx`, `notifications/page.tsx`, `projects/map/page.tsx`, `settings/team/page.tsx`, and `document-parse-status.tsx` are likely intentional but should be reviewed periodically. Consider adding `// eslint-disable-next-line` with a comment explaining why the dep is excluded.

2. **Stale worktrees:** The `.claude/worktrees/` directory contains 3 worktrees (`zealous-wozniak`, `pedantic-brattain`, `priceless-kapitsa`) that still have `any` type casts from before the lint cleanup. These don't affect production but add noise to searches. Consider cleaning them up with `git worktree remove`.

3. **Build verification:** The sandbox FUSE filesystem prevents running `next build` locally in this environment. The Vercel deployment serves as the authoritative build check. If local build verification is needed, consider adding a CI step.

---

*Report generated automatically by EntitleFlow QA scheduled task.*
