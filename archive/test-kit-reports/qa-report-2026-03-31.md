# EntitleFlow QA Report — 2026-03-31

**Run type:** Scheduled autonomous sweep
**Branch:** main
**Latest commit:** `0dc0f326` — fix: profile avatar not rendering in portal topbar

---

## Summary

| Check | Status |
|---|---|
| TypeScript typecheck | **PASS** — 0 errors |
| ESLint | **PASS** — 0 errors, 7 warnings |
| Production build (Vercel) | **PASS** — latest deployment READY |
| Production build (sandbox) | **SKIP** — EPERM on `.next/BUILD_ID` (sandbox filesystem limitation, not a code issue) |
| Deployment status | **PASS** — `dpl_Er4x6jaHVYRs6NzeWEtG5rT4sFCz` READY, production target |
| Database schema drift | **PASS** — all 35 tables in sync between Supabase and `database.types.ts` |

---

## Issues Found

### Critical: None

### Warning

#### 1. Stale Git Worktrees Contain Legacy Issues (severity: warning)

Three worktrees under `.claude/worktrees/` (zealous-wozniak, pedantic-brattain, priceless-kapitsa) contain stale code with known issues that have already been fixed on `main`:

- **69 `any` type usages** across 42 files (all in worktrees, 0 in main)
- **`organization_members` table references** in worktree `app/api/admin/users/route.ts` (this table doesn't exist — fixed on main to use `team_members`)
- Duplicate copies of all components, inflating grep/search results

**Recommendation:** Clean up stale worktrees to reduce confusion and disk usage:
```bash
cd .claude/worktrees && rm -rf zealous-wozniak pedantic-brattain priceless-kapitsa
```

#### 2. ESLint Warnings — Missing Hook Dependencies (severity: warning)

7 `react-hooks/exhaustive-deps` warnings in 4 files:

| File | Count |
|---|---|
| `app/app/documents/page.tsx` | 1 (missing `supabase`) |
| `app/app/notifications/page.tsx` | 1 (missing `supabase`) |
| `app/app/projects/map/page.tsx` | 1 (missing `loadProjects`) |
| `app/app/settings/team/page.tsx` | 1 (missing `loadTeamData`) |
| `components/app/document-parse-status.tsx` | 3 (missing `onParseComplete`, `statusInfo.parse_status`, `fetchStatus`) |

These are non-blocking but could cause stale closure bugs in edge cases. The `supabase` client is stable so those two are safe to suppress. The `document-parse-status.tsx` warnings are more likely to cause real bugs if callbacks change.

#### 3. `dark:` Usage in Components (severity: info)

6 occurrences of `dark:` found in main codebase, but none are Tailwind dark-mode classes:

- `components/site/Logo.tsx` (line 27): Object property `dark: {}` for logo color variants — **not a violation**
- `components/ui/onboarding-dialog.tsx` (lines 13, 22, 27, 32, 37): Object property `dark: string` for step color palette — **not a violation**

**Result: No dark-mode Tailwind violations found. PASS.**

---

## Security Audit

| Check | Status | Details |
|---|---|---|
| `SUPABASE_SERVICE_ROLE_KEY` in client code | **PASS** | Only referenced in `lib/supabase/server.ts`, `lib/admin/auth.ts`, and `app/api/email/inbound/route.ts` — all server-only files. No `'use client'` files reference it. |
| API route auth guards | **PASS** | All 61 API routes verified. Admin routes use `verifyAdmin()`. Company-admin routes use `verifyCompanyAdmin()`. App routes use `getUser()`. Public routes (`/api/leads`) are intentionally unauthenticated. |
| `organization_id` scoping in company-admin | **PASS** | All company-admin routes consistently filter by `admin.organization_id`. Verified across security, groups, groups/members, permissions, storage, audit, license-requests, users, and stats routes. |
| Incorrect table references (main) | **PASS** | No references to `audit_logs`, `organization_members`, `organization_licenses`, `password_reset_tokens`, `user_licenses`, or `license_assignment_log` in main codebase. |
| `any` type usage (main) | **PASS** | 0 occurrences in main codebase (all 69 are in stale worktrees). |

---

## Deployment Verification

- **Latest deployment:** `dpl_Er4x6jaHVYRs6NzeWEtG5rT4sFCz`
- **State:** READY (production)
- **Commit:** `0dc0f326` — fix: profile avatar not rendering in portal topbar
- **Created:** 2026-03-31
- **Bundler:** Turbopack

Last 20 deployments show a healthy pattern: 15 READY, 5 ERROR (all historical, already resolved by subsequent commits). No current deployment issues.

---

## Database Schema Drift Check

**35 tables in Supabase** match exactly with **35 table definitions in `lib/database.types.ts`**:

activity_log, admin_audit_log, announcements, comment_assignments, comments, company_group_members, company_groups, contract_change_usage, deadlines, documents, email_queue, feature_flags, flowe_conversations, flowe_knowledge, flowe_messages, jurisdictions, license_change_requests, license_definitions, marketing_leads, notification_preferences, notifications, organization_contracts, organizations, parse_jobs, password_reset_config, permit_status_history, permits, platform_config, profiles, projects, role_permissions, team_invitations, team_members, user_activity_tracking, user_permission_overrides

Types file also includes 3 database functions: `check_user_permission`, `is_org_admin`, `match_flowe_knowledge`.

**Result: No schema drift detected. PASS.**

---

## Metrics

| Metric | Value |
|---|---|
| TypeScript errors | 0 |
| ESLint errors | 0 |
| ESLint warnings | 7 |
| `any` type usages (main) | 0 |
| `any` type usages (worktrees) | 69 |
| Dark-mode Tailwind violations | 0 |
| Unprotected API routes | 0 |
| Missing org-scope filters | 0 |
| Service role key exposures | 0 |
| Incorrect table references (main) | 0 |
| Database tables in Supabase | 35 |
| Database tables in types file | 35 |
| Schema drift mismatches | 0 |
| Total API routes | 61 |
| Production deployments (last 20) | 15 READY, 5 ERROR (historical) |

---

## Fixes Applied

None required — all checks passed on main. No code changes made.

---

## Recommendations

1. **Clean up stale worktrees** — The 3 worktrees under `.claude/worktrees/` are outdated copies that inflate search results and contain fixed bugs. Remove them to reduce noise.

2. **Address exhaustive-deps warnings in `document-parse-status.tsx`** — The 3 warnings here (missing `onParseComplete`, `statusInfo.parse_status`, `fetchStatus`) could cause stale closure bugs during real-time document parsing status polling. Consider wrapping callbacks in `useCallback` at the parent level or adding the deps.

3. **Consider `.eslintignore` update** — The current `.eslintignore` may not be fully excluding `.claude/worktrees/` (the ESLint flat config migration warning suggests the `.eslintignore` file format is deprecated). Migrate ignores to `eslint.config.js` `ignores` array.

4. **Email inbound route auth** — `app/api/email/inbound/route.ts` uses the admin client directly without user auth. This is expected for a webhook endpoint, but consider adding a webhook secret/signature verification to prevent unauthorized POST requests.

---

*Report generated by scheduled QA agent — 2026-03-31*
