# EntitleFlow QA Report — 2026-04-04

**Run type:** Automated scheduled sweep
**Runner:** Claude Opus 4.6 (Cowork agent)
**Commit:** `0dc0f3262b` (main) — "fix: profile avatar not rendering in portal topbar"

---

## Summary

| Check | Status |
|---|---|
| TypeScript typecheck | **PASS** — 0 errors |
| ESLint | **PASS** — 0 errors, 7 warnings |
| Production build (Vercel) | **PASS** — latest deployment READY |
| Production build (sandbox) | **SKIP** — FUSE mount permission error (sandbox limitation, not code issue) |
| Vercel deployment | **PASS** — `dpl_Er4x6jaHVYRs6NzeWEtG5rT4sFCz` READY on production |
| Database schema drift | **PASS** — 35 tables match between types and live DB |

---

## Phase 1: Static Analysis

### TypeScript Typecheck
**Result: PASS** — `tsc --noEmit` completed with zero errors.

### ESLint
**Result: PASS** — 0 errors, 7 warnings (all `react-hooks/exhaustive-deps`).

Warnings in:
- `app/app/documents/page.tsx:326` — missing dep `supabase`
- `app/app/notifications/page.tsx:284` — missing dep `supabase`
- `app/app/projects/map/page.tsx:46` — missing dep `loadProjects`
- `app/app/settings/team/page.tsx:150` — missing dep `loadTeamData`
- `components/app/document-parse-status.tsx:108` — missing deps `onParseComplete`, `statusInfo.parse_status`
- `components/app/document-parse-status.tsx:149` — missing dep `fetchStatus`
- `components/app/document-parse-status.tsx:167` — missing dep `fetchStatus`

**Recommendation:** These are intentional stable-reference patterns (callback refs and initial-load effects). No action needed unless unexpected re-render bugs appear.

### Production Build
The sandbox build failed with `EPERM: operation not permitted` on a FUSE-mounted `.next/` hidden file — a sandbox filesystem limitation, not a code defect. The Vercel production build (same commit) succeeded and deployed cleanly.

---

## Phase 2: Code Quality Audit

### Dark Mode Violation Scan
**Result: PASS** — No Tailwind `dark:` variant classes found.

The `dark:` string appears in 2 main-codebase files, but these are JavaScript object property names (color configuration data), not Tailwind CSS classes:
- `components/site/Logo.tsx:27` — `dark: { ... }` (logo color variant object)
- `components/ui/onboarding-dialog.tsx:13,22,27,32,37` — `colors: { dark: '#0f3c35', light: '#e8f5f2' }` (step color config)

### API Route Auth Checks
**Result: PASS** — All protected routes verified.

- **Super-admin routes** (`app/api/admin/*`): All use `verifyAdmin()` guard
- **Company-admin routes** (`app/api/company-admin/*`): All use `verifyCompanyAdmin()` guard
- **User-facing routes** (comments, documents, notifications, tasks, team): All use `getUser()` pattern
- **Special routes**: `geocode` and `email/send` properly require auth
- **Public routes**: `leads` correctly has no auth (marketing form)

### Service Role Key Exposure
**Result: PASS** — `SUPABASE_SERVICE_ROLE_KEY` appears in code only in server-side files:
- `lib/supabase/server.ts` (server utility)
- `lib/admin/auth.ts` (admin verification)
- `app/api/email/inbound/route.ts` (server route handler)

No `'use client'` files reference the service role key.

### Incorrect Table References
**Result: PASS** — No phantom table names (`audit_logs`, `organization_members`, `organization_licenses`, `password_reset_tokens`, `user_licenses`, `license_assignment_log`) found in API route code. The only match for `licenses` is a variable name in response shaping (not a table query).

### `any` Type Usage
**Result: PASS** — **0 occurrences** of `: any` in the main codebase (`app/`, `lib/`, `components/`).

69 occurrences exist in `.claude/worktrees/` (stale worktree copies from previous Claude Code sessions — these are not deployed).

### Organization ID Scoping (Company-Admin Routes)
**Result: PASS** — All 12 route files in `app/api/company-admin/` properly filter queries by `organization_id`:
- `users/route.ts` — filters profiles by org_id
- `users/[id]/route.ts` — verifies user org membership before access
- `users/[id]/reset-password/route.ts` — verifies user belongs to org
- `license-requests/route.ts` — scopes requests by org_id
- `security/route.ts` — queries/updates config by org_id
- `groups/route.ts` — filters groups by org_id, verifies parent-group org membership
- `permissions/route.ts`, `audit/route.ts`, `storage/route.ts`, `stats/route.ts` — all org-scoped

---

## Phase 3: Deployment Verification

### Latest Deployment
- **ID:** `dpl_Er4x6jaHVYRs6NzeWEtG5rT4sFCz`
- **State:** READY
- **Target:** production
- **Commit:** `0dc0f3262b` — "fix: profile avatar not rendering in portal topbar"
- **Bundler:** Turbopack
- **Inspector:** https://vercel.com/entitleflow-nc/entitle-flow/Er4x6jaHVYRs6NzeWEtG5rT4sFCz

All recent production deployments are READY. Historical ERROR deployments (4 total in the last 20) were all resolved in subsequent commits.

---

## Phase 4: Database Schema Drift

**Result: PASS** — Perfect 35-table match between `lib/database.types.ts` and live Supabase.

| Table | In Types | In Live DB |
|---|---|---|
| activity_log | ✓ | ✓ |
| admin_audit_log | ✓ | ✓ |
| announcements | ✓ | ✓ |
| comment_assignments | ✓ | ✓ |
| comments | ✓ | ✓ |
| company_group_members | ✓ | ✓ |
| company_groups | ✓ | ✓ |
| contract_change_usage | ✓ | ✓ |
| deadlines | ✓ | ✓ |
| documents | ✓ | ✓ |
| email_queue | ✓ | ✓ |
| feature_flags | ✓ | ✓ |
| flowe_conversations | ✓ | ✓ |
| flowe_knowledge | ✓ | ✓ |
| flowe_messages | ✓ | ✓ |
| jurisdictions | ✓ | ✓ |
| license_change_requests | ✓ | ✓ |
| license_definitions | ✓ | ✓ |
| marketing_leads | ✓ | ✓ |
| notification_preferences | ✓ | ✓ |
| notifications | ✓ | ✓ |
| organization_contracts | ✓ | ✓ |
| organizations | ✓ | ✓ |
| parse_jobs | ✓ | ✓ |
| password_reset_config | ✓ | ✓ |
| permit_status_history | ✓ | ✓ |
| permits | ✓ | ✓ |
| platform_config | ✓ | ✓ |
| profiles | ✓ | ✓ |
| projects | ✓ | ✓ |
| role_permissions | ✓ | ✓ |
| team_invitations | ✓ | ✓ |
| team_members | ✓ | ✓ |
| user_activity_tracking | ✓ | ✓ |
| user_permission_overrides | ✓ | ✓ |

---

## Fixes Applied

None required — all checks passed.

---

## Recommendations

| Priority | Item | Details |
|---|---|---|
| **Info** | Clean stale worktrees | `.claude/worktrees/` contains 3 stale worktrees (zealous-wozniak, priceless-kapitsa, pedantic-brattain) with 69 `: any` occurrences. Run `git worktree prune` and delete the directories to reduce noise in future scans. |
| **Info** | ESLint exhaustive-deps warnings | 7 warnings are stable-reference patterns. Consider adding `// eslint-disable-next-line` with explanatory comments if they're intentional to keep the warning count at 0. |
| **Info** | ESLint config migration | `.eslintignore` is deprecated in ESLint flat config. Migrate ignore patterns to the `ignores` property in `eslint.config.js`. |

---

## Metrics

| Metric | Value |
|---|---|
| TypeScript errors | 0 |
| ESLint errors | 0 |
| ESLint warnings | 7 |
| `: any` types (main codebase) | 0 |
| `: any` types (worktrees, not deployed) | 69 |
| Dark mode violations | 0 |
| Unprotected API routes | 0 |
| Org-scoping violations | 0 |
| Service role key in client code | 0 |
| Phantom table references | 0 |
| Schema drift mismatches | 0 |
| Vercel deployment status | READY |
| Supabase tables (live) | 35 |
| Tables in types file | 35 |
