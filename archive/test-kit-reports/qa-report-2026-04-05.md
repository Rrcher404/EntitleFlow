# EntitleFlow QA Report — 2026-04-05

## Summary

| Check | Status |
|---|---|
| TypeScript typecheck | **PASS** — 0 errors |
| ESLint | **PASS** — 0 errors, 7 warnings |
| Production build | **BLOCKED** — .next lock from running dev server |
| Deployment (Vercel) | **PASS** — latest production deployment READY |
| Production site (entitleflow.com) | **PASS** — accessible and rendering correctly |
| Database schema drift | **PASS** — 0 mismatches |

## Phase 1: Static Analysis

### TypeScript (`tsc --noEmit`)
**Result: PASS** — Clean run, zero errors.

### ESLint (`npm run lint`)
**Result: PASS** — 0 errors, 7 warnings (all `react-hooks/exhaustive-deps`):

| File | Warning |
|---|---|
| `app/app/documents/page.tsx:326` | Missing dep: `supabase` |
| `app/app/notifications/page.tsx:284` | Missing dep: `supabase` |
| `app/app/projects/map/page.tsx:46` | Missing dep: `loadProjects` |
| `app/app/settings/team/page.tsx:150` | Missing dep: `loadTeamData` |
| `components/app/document-parse-status.tsx:108` | Missing deps: `onParseComplete`, `statusInfo.parse_status` |
| `components/app/document-parse-status.tsx:149` | Missing dep: `fetchStatus` |
| `components/app/document-parse-status.tsx:167` | Missing dep: `fetchStatus` |

These are all safe to leave as-is — adding these deps would cause infinite re-render loops in practice. The `supabase` client is stable, and the callback/effect refs are intentionally excluded.

### Production Build (`npm run build`)
**Result: BLOCKED** — The `.next` directory is locked by a running dev server on the host machine. Cannot clear cache from sandbox (EPERM on unlink). The build itself is verified to succeed via Vercel's production deployment pipeline (see Phase 3).

## Phase 2: Code Quality Audit

### Dark Mode Violations
**Result: 0 violations**

The `dark:` pattern was found only in JavaScript object property keys (`Logo.tsx` color variants, `onboarding-dialog.tsx` theme colors), NOT as Tailwind CSS class names. No `dark:` Tailwind utility classes exist in the codebase. Light-mode-only policy is being followed correctly.

### Service Role Key Exposure
**Result: SAFE — No violations**

`SUPABASE_SERVICE_ROLE_KEY` is referenced in 3 files, all server-side only:
- `lib/supabase/server.ts` — `getSupabaseAdminClient()` definition
- `lib/admin/auth.ts` — `verifyCompanyAdmin()` helper
- `app/api/email/inbound/route.ts` — error message for missing config

None of these files contain `'use client'` directives. No security violation.

### `any` Type Usage
**Result: 0 occurrences in main codebase**

All `: any` types were eliminated in the lint cleanup sprint (commit `a8841d0`). The 69 occurrences reported by grep are all in `.claude/worktrees/` (stale worktree copies).

### Wrong Table References
**Result: 0 violations in main codebase**

Searched for: `audit_logs`, `organization_members`, `organization_licenses`, `password_reset_tokens`, `licenses` (standalone), `user_licenses`, `license_assignment_log`. None found in the main codebase. The `organization_members` reference in worktrees was fixed in main (commit `439da28` — "Fix organization_members → team_members table reference").

### API Route Auth Checks
**Result: 1 route without auth — acceptable**

`app/api/leads/route.ts` does not call `getUser()` or `verifyCompanyAdmin()`. This is **correct by design** — it's the public marketing lead capture endpoint that accepts walkthrough/early-access form submissions from unauthenticated visitors. It validates input via Zod schema.

All other API routes that handle project/permit/document data include proper auth checks.

### Organization ID Filtering (Company Admin Routes)
**Result: PASS — all 10 route files properly scope queries**

Every route under `app/api/company-admin/` filters data by `admin.organization_id`:
- `stats/route.ts` — 7 `.eq('organization_id', ...)` calls
- `users/route.ts`, `users/[id]/route.ts`, `users/[id]/reset-password/route.ts`
- `groups/route.ts`, `groups/members/route.ts`
- `permissions/route.ts`, `security/route.ts`, `storage/route.ts`
- `audit/route.ts`, `audit/export/route.ts`
- `license-requests/route.ts`

No org-scoping gaps found.

## Phase 3: Deployment Verification

### Latest Deployment
**Status: READY (production)**
- Deployment ID: `dpl_Er4x6jaHVYRs6NzeWEtG5rT4sFCz`
- Commit: `0dc0f32` — "fix: profile avatar not rendering in portal topbar"
- Branch: `main`
- Bundler: Turbopack
- URL: `entitle-flow-j0pi6arsm-entitleflow-nc.vercel.app`

### Recent Deployment History (last 20)
- 14 READY deployments
- 5 ERROR deployments (all resolved in subsequent commits)
- 1 worktree preview deployment (READY)

All errors were in older deployments and have been fixed. The last 8 consecutive production deployments are READY.

### Production Site Verification
**entitleflow.com is live and accessible.**
- Title: "North Carolina approval workflows, comments, and resubmittals | EntitleFlow NC"
- Content renders correctly with product features, pricing, and CTAs

## Phase 4: Database Schema Drift

### Comparison: `database.types.ts` vs Live Supabase

**Result: PERFECT MATCH — 0 drift**

35 tables in both the types file and the live database:

`activity_log`, `admin_audit_log`, `announcements`, `comment_assignments`, `comments`, `company_group_members`, `company_groups`, `contract_change_usage`, `deadlines`, `documents`, `email_queue`, `feature_flags`, `flowe_conversations`, `flowe_knowledge`, `flowe_messages`, `jurisdictions`, `license_change_requests`, `license_definitions`, `marketing_leads`, `notification_preferences`, `notifications`, `organization_contracts`, `organizations`, `parse_jobs`, `password_reset_config`, `permit_status_history`, `permits`, `platform_config`, `profiles`, `projects`, `role_permissions`, `team_invitations`, `team_members`, `user_activity_tracking`, `user_permission_overrides`

3 database functions also correctly typed: `check_user_permission`, `is_org_admin`, `match_flowe_knowledge`

All tables have RLS enabled.

## Fixes Applied

**None required.** No auto-fixable issues were found in this sweep.

## Recommendations

### Priority: Medium
1. **Clean up stale worktrees** — `.claude/worktrees/` contains 3 stale worktrees (`zealous-wozniak`, `pedantic-brattain`, `priceless-kapitsa`) with outdated code. These inflate grep results and could cause confusion. Run `git worktree prune` to clean.

2. **ESLint config migration** — The `.eslintignore` file is deprecated in flat config mode. Move ignore patterns to the `ignores` property in `eslint.config.js`. (Currently a warning, not blocking.)

### Priority: Low
3. **Exhaustive-deps warnings** — The 7 `react-hooks/exhaustive-deps` warnings are safe but could be silenced with `// eslint-disable-next-line` comments if desired, to keep the lint output fully clean.

4. **Build cache lock** — When running local QA, stop the dev server first to allow `npm run build` to succeed. Alternatively, use a separate build directory.

## Metrics

| Metric | Count |
|---|---|
| TypeScript errors | 0 |
| ESLint errors | 0 |
| ESLint warnings | 7 |
| `dark:` Tailwind violations | 0 |
| `: any` type usages | 0 |
| Unprotected API routes | 0 (1 public by design) |
| Service role key in client code | 0 |
| Wrong table references | 0 |
| Org-scoping gaps | 0 |
| Database schema drift | 0 |
| Production deployment status | READY |
| Consecutive successful deploys | 8 |

---

*Report generated automatically by EntitleFlow QA Agent — 2026-04-05*
