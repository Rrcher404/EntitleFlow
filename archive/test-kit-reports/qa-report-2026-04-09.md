# EntitleFlow QA Report — 2026-04-09

**Run type:** Automated scheduled sweep
**Agent:** Claude Opus 4.6 (Cowork)
**Commit:** `0dc0f32` (fix: profile avatar not rendering in portal topbar)

---

## Summary

| Check | Status |
|---|---|
| TypeScript typecheck | **PASS** — 0 errors |
| ESLint | **PASS** — 0 errors, 7 warnings |
| Production build | **SKIP** — sandbox FUSE filesystem limitation (EPERM on `.next/.fuse_hidden*`); Vercel builds confirmed READY |
| Latest deployment | **PASS** — `dpl_Er4x6jaHVYRs6NzeWEtG5rT4sFCz` is READY (production) |
| Production site (entitleflow.com) | **PASS** — site loads correctly, title: "North Carolina approval workflows, comments, and resubmittals \| EntitleFlow NC" |
| Database schema drift | **PASS** — 35 tables in DB, 35 tables in `database.types.ts`, 0 mismatches |
| Service role key exposure | **PASS** — no `'use client'` files reference `SUPABASE_SERVICE_ROLE_KEY` |
| Dark mode violations | **PASS** — 0 `dark:` Tailwind utility classes found in main source |
| `any` type count | **PASS** — 0 instances in main source (app/, lib/, components/ excluding `.claude/` worktrees) |

---

## Phase 1: Static Analysis

### TypeScript (`tsc --noEmit`)
**Result: PASS** — Clean compile, zero errors.

### ESLint (`npm run lint`)
**Result: PASS** — 0 errors, 7 warnings (all `react-hooks/exhaustive-deps`):

| File | Warning |
|---|---|
| `app/app/documents/page.tsx:326` | Missing dependency: `supabase` |
| `app/app/notifications/page.tsx:284` | Missing dependency: `supabase` |
| `app/app/projects/map/page.tsx:46` | Missing dependency: `loadProjects` |
| `app/app/settings/team/page.tsx:150` | Missing dependency: `loadTeamData` |
| `components/app/document-parse-status.tsx:108` | Missing dependencies: `onParseComplete`, `statusInfo.parse_status` |
| `components/app/document-parse-status.tsx:149` | Missing dependency: `fetchStatus` |
| `components/app/document-parse-status.tsx:167` | Missing dependency: `fetchStatus` |

These are intentional omissions to prevent infinite re-render loops. No action needed.

### Production Build
**Result: SKIP** — The sandbox environment uses a FUSE filesystem mount that prevents `next build` from unlinking cache files (`EPERM: operation not permitted`). This is a sandbox limitation, not a codebase issue. Vercel's latest production build succeeded (see Phase 3).

---

## Phase 2: Code Quality Audit

### Dark Mode Violations
**Result: PASS** — No `dark:` Tailwind utility classes found in main source files. The `dark:` string appears only in:
- `components/site/Logo.tsx` — as a JS object key for logo color variants (not a Tailwind class)
- `components/ui/onboarding-dialog.tsx` — as a JS object property for color definitions (not a Tailwind class)

### API Route Auth Checks
**Result: PASS** — All API routes have appropriate authentication:

- **Public routes (by design):** `app/api/leads/route.ts` — lead capture form, intentionally unauthenticated
- **Admin routes:** All 12 admin API routes use `verifyAdmin()` from `lib/admin/auth.ts`, which calls `getUser()` to verify JWT and checks `is_super_admin` on the profile before returning the service client
- **Company-admin routes:** All 12 routes use `verifyOrganizationAccess()` or equivalent org-scoped auth
- **App API routes:** All routes (comments, tasks, documents, AI, team, notifications, email) verify auth via `createServerSupabaseClient()` + `getUser()`

### Service Role Key Exposure
**Result: PASS** — `SUPABASE_SERVICE_ROLE_KEY` appears in code only in server-side files:
- `lib/supabase/server.ts` — `getSupabaseAdminClient()`
- `lib/admin/auth.ts` — `verifyAdmin()`
- `app/api/email/inbound/route.ts` — server-only route handler

No `'use client'` files reference the key.

### Organization ID Filters (Company-Admin Routes)
**Result: PASS** — All 12 company-admin API routes include `organization_id` filtering in their queries.

### Incorrect Table References
**Result: INFO** — The word "licenses" appears in admin UI code (`app/admin/licenses/page.tsx`, `app/admin/organizations/[id]/page.tsx`) as UI labels and API route names, but these reference the `license_definitions` table in the actual queries, not a nonexistent `licenses` table. No actual incorrect table references found in query code.

### `any` Type Usage
**Result: PASS** — **0 instances** of `: any` in the main source tree (`app/`, `lib/`, `components/`). The previous lint cleanup sprint on 2026-04-08 eliminated all `any` casts. The 69 instances found by the initial grep are all in `.claude/worktrees/` directories (stale worktree copies).

---

## Phase 3: Deployment Verification

### Latest Deployment
**Result: PASS**

| Field | Value |
|---|---|
| Deployment ID | `dpl_Er4x6jaHVYRs6NzeWEtG5rT4sFCz` |
| State | **READY** |
| Target | Production |
| Commit | `0dc0f32` — "fix: profile avatar not rendering in portal topbar" |
| URL | `entitle-flow-j0pi6arsm-entitleflow-nc.vercel.app` |
| Bundler | Turbopack |

The last 10 production deployments are all READY. Historical ERROR deployments (from the RBAC migration sprint on ~Mar 21) were quickly resolved with follow-up commits.

### Production Site
**Result: PASS** — `entitleflow.com` loads correctly with proper title, navigation, and content sections.

---

## Phase 4: Database Schema Drift

**Result: PASS** — Perfect alignment between live database and `lib/database.types.ts`.

**35 tables in both:**
`activity_log`, `admin_audit_log`, `announcements`, `comment_assignments`, `comments`, `company_group_members`, `company_groups`, `contract_change_usage`, `deadlines`, `documents`, `email_queue`, `feature_flags`, `flowe_conversations`, `flowe_knowledge`, `flowe_messages`, `jurisdictions`, `license_change_requests`, `license_definitions`, `marketing_leads`, `notification_preferences`, `notifications`, `organization_contracts`, `organizations`, `parse_jobs`, `password_reset_config`, `permit_status_history`, `permits`, `platform_config`, `profiles`, `projects`, `role_permissions`, `team_invitations`, `team_members`, `user_activity_tracking`, `user_permission_overrides`

All tables have RLS enabled.

---

## Fixes Applied

**None required.** The codebase is clean — zero TypeScript errors, zero ESLint errors, zero `any` types, zero dark mode violations, zero security issues.

---

## Recommendations

| Priority | Item | Details |
|---|---|---|
| **Info** | Clean up stale worktrees | `.claude/worktrees/` contains 3 old worktrees (zealous-wozniak, pedantic-brattain, priceless-kapitsa) with 69 `any` type instances. These don't affect production but add noise to grep results. Run `git worktree prune` and remove manually. |
| **Info** | ESLint config modernization | The `.eslintignore` file triggers a deprecation warning. Migrate ignores to the `ignores` property in `eslint.config.js`. |
| **Info** | Sandbox build limitation | `npm run build` cannot run in the Cowork sandbox due to FUSE filesystem limitations. This doesn't affect Vercel deployments but limits local QA validation. |

---

## Metrics

| Metric | Value |
|---|---|
| TypeScript errors | 0 |
| ESLint errors | 0 |
| ESLint warnings | 7 (exhaustive-deps) |
| `any` type instances (main source) | 0 |
| `any` type instances (worktrees) | 69 |
| Dark mode violations | 0 |
| Unprotected API routes | 0 (leads route is intentionally public) |
| Service role key in client code | 0 |
| Missing org_id filters | 0 |
| Database tables | 35 |
| Schema drift mismatches | 0 |
| Tables with RLS enabled | 35/35 |
| Recent deployments (READY) | 10/10 (last 10 production) |
