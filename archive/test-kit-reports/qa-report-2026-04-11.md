# EntitleFlow QA Report — 2026-04-11

Automated QA sweep run by the `entitleflow-deep-test` scheduled task.

## Summary

| Check | Result |
|---|---|
| TypeScript (`tsc --noEmit`) | **PASS** — 0 errors |
| ESLint (`npm run lint`) | **PASS** — 0 errors, 7 warnings (exhaustive-deps only) |
| Production build (`npm run build`) | **INCONCLUSIVE locally** — sandbox FUSE blocked `.next` unlink; Vercel production build on the same commit is **READY** |
| Vercel latest prod deployment | **READY** — `dpl_Er4x6jaHVYRs6NzeWEtG5rT4sFCz` (commit `0dc0f32`, "fix: profile avatar not rendering in portal topbar") |
| Production URL (`https://entitleflow.com`) | **200 OK** (~0.4s) |
| Database schema drift | **PASS** — no code in `app/` or `lib/` references phantom tables |
| Security: service-role key in client code | **PASS** — only referenced in server-only files |
| Auth checks on data-touching API routes | **PASS** — only intentional exceptions unprotected |
| Dark mode violations | **PASS** — zero `dark:` Tailwind variants |
| `any` type usage | **PASS** — 0 occurrences in `app/`, `lib/`, `components/` |
| `organization_id` scoping on company-admin routes | **PASS** — no un-scoped queries on `projects`/`permits`/`documents`/`profiles` |

No auto-fixes were applied. Nothing required them.

## Phase 1 — Static Analysis

### TypeScript
```
./node_modules/.bin/tsc --noEmit
```
Exit: 0. No diagnostics output.

### ESLint
```
npm run lint  →  0 errors, 7 warnings
```
All 7 warnings are `react-hooks/exhaustive-deps` — intentional patterns in files with stable `supabase` clients or explicit fetch callbacks:

- `app/app/documents/page.tsx:326` — missing `supabase` dep in `useCallback`
- `app/app/notifications/page.tsx:284` — missing `supabase` dep in `useCallback`
- `app/app/projects/map/page.tsx:46` — missing `loadProjects` dep in `useEffect`
- `app/app/settings/team/page.tsx:150` — missing `loadTeamData` dep in `useEffect`
- `components/app/document-parse-status.tsx:108` — missing `onParseComplete`, `statusInfo.parse_status` deps
- `components/app/document-parse-status.tsx:149` — missing `fetchStatus` dep
- `components/app/document-parse-status.tsx:167` — missing `fetchStatus` dep

These have been present across multiple prior QA reports. They do not affect runtime behavior. Not auto-fixed — flagged for human review only.

Note: ESLint still emits `.eslintignore file is no longer supported` migration warning. The ignore list lives in both `.eslintignore` and `eslint.config.js`. Consider consolidating into `eslint.config.js` `ignores` property.

### Production Build
Attempted `npm run build` in the Linux sandbox. The build fails with:

```
Error: EPERM: operation not permitted, unlink
  '/Users/jyecemaybury/PermitPilot/.next/BUILD_ID'
```

This is a sandbox filesystem limitation — the `.next` directory on the host Mac is owned by the host user and the sandbox's FUSE mount cannot `unlink` it. It is **not** a code-level build failure. Evidence: the latest `main` commit (`0dc0f32`) built successfully on Vercel and is serving production. See Phase 3.

A retry with `NEXT_DIST_DIR=/tmp/next-build` still failed because Next.js walks the repo's existing `.next` during prep. The earlier 2026-04-11 report run (generated from a different environment at 13:06 local) did complete the local build successfully in 12.0s — so the codebase itself builds cleanly, only this environment cannot clean its own artifacts.

**Recommendation**: for future automated runs, either clear `.next` from the host before triggering, or rely on the Vercel deployment state as the authoritative build signal.

## Phase 2 — Code Quality Audit

### Dark mode violation scan
Zero Tailwind `dark:` variants in source code.

The only `dark` string matches are a property key (`dark: '#0f3c35'`) in `components/ui/onboarding-dialog.tsx` — a color-pair object (dark/light hex values), not a Tailwind variant. And `components/site/Logo.tsx` references a `dark` variant of the logo artwork, not a theme class. Both are semantically fine.

### Service role key exposure
`SUPABASE_SERVICE_ROLE_KEY` appears in runtime code only in:

- `lib/supabase/server.ts:36`
- `lib/admin/auth.ts:21`
- `app/api/email/inbound/route.ts:79`

All three are server-only. No file with a `'use client'` directive references the key. **PASS.**

### Known-wrong table name scan
Searched `app/` and `lib/` for: `audit_logs`, `organization_members`, `organization_licenses`, `password_reset_tokens`, `user_licenses`, `license_assignment_log`.

Main-tree result: **zero hits**. The only matches are in `.claude/worktrees/{zealous-wozniak, priceless-kapitsa, pedantic-brattain}/app/api/admin/users/route.ts`, which are stale worktrees under `.claude/`, not the active codebase. Not a production issue.

### Auth checks on API routes
Scanned all 50+ route handlers under `app/api/`. Every handler that touches permit, project, document, comment, notification, team, or admin data calls one of: `supabase.auth.getUser()`, `verifyCompanyAdmin()`, `verifyAdmin()`, or `requireAuth()` before performing data operations.

Unprotected routes (intentional):
- `app/api/leads/route.ts` — public lead capture endpoint (marketing form)
- `app/api/email/inbound/route.ts` — inbound email webhook (own token-based auth path)

**PASS.**

### `any` type usage
Count in `app/`, `lib/`, `components/`: **0**. A prior cleanup sprint removed all 196 vestigial `as any` casts (see commit `a8841d0`). Nothing to flag.

### `organization_id` scoping
Scanned `app/api/company-admin/**/route.ts` for queries to `projects`, `permits`, `documents`, `profiles` tables. Every query that returns multi-row data includes `.eq('organization_id', ...)` filtering. **PASS.**

## Phase 3 — Deployment Verification

Latest production deployment: `dpl_Er4x6jaHVYRs6NzeWEtG5rT4sFCz`
- State: **READY**
- Commit: `0dc0f32` — *"fix: profile avatar not rendering in portal topbar"*
- Target: production
- Branch: `main`

Previous deployment history (last 20 pulled): 15 READY, 4 historical ERROR states (all followed by successful recovery commits), 1 non-production preview. The 3 most recent production deployments are all READY. No action needed.

Production URL probe:
```
curl -I https://entitleflow.com  →  HTTP 200, ~395ms
```

**PASS.**

## Phase 4 — Database Schema Drift Check

Queried Supabase `list_tables` on project `sjyzqqratghsjttqeghy`. 35 tables in `public` schema. The following tables referenced in `lib/database.types.ts` were verified to exist:

`marketing_leads, organizations, profiles, projects, permits, permit_status_history, comments, documents, activity_log, deadlines, jurisdictions, platform_config, feature_flags, announcements, admin_audit_log, team_members, team_invitations, comment_assignments, notifications, notification_preferences, parse_jobs, email_queue, flowe_conversations, flowe_messages, flowe_knowledge, license_definitions, role_permissions, user_permission_overrides, password_reset_config, user_activity_tracking, company_groups, company_group_members, license_change_requests, organization_contracts, contract_change_usage`

Phantom tables (`audit_logs`, `organization_members`, `organization_licenses`, `password_reset_tokens`, `licenses`, `user_licenses`, `license_assignment_log`) are correctly absent from both the DB and the live code. **No drift.**

## Issues Found

### Critical
None.

### Warning
1. **7 exhaustive-deps React hook warnings** — pre-existing, behaviorally intentional but worth a future pass to either add the missing deps or add `eslint-disable-next-line` with justifications. Files listed under Phase 1.
2. **`.eslintignore` deprecation warning** — harmless today, will break on the next ESLint major. Move ignores into `eslint.config.js`.

### Info
1. **Sandbox build environment cannot `unlink` `.next`** — not a code issue. For future automated QA runs, pre-clear `.next` on the host or skip the local build step in favor of the Vercel deployment-state signal.

## Fixes Applied

None. Nothing required auto-fixing this run.

## Recommendations

1. **Flip `.eslintignore` → `eslint.config.js` ignores**. ~5-minute change, removes the deprecation noise and future-proofs the lint step.
2. **Clean up the 7 exhaustive-deps warnings** when touching those files for other reasons. Not urgent.
3. **Consider a workflow to prune stale `.claude/worktrees/`** — they're producing noise in repo-wide greps and storing out-of-date copies of admin routes that reference phantom tables. Not a bug, just clutter.
4. **For future scheduled QA runs, update the build step** to tolerate the sandbox's FUSE constraints (e.g., skip if `.next` cannot be unlinked and rely on Vercel).

## Metrics

| Metric | Value |
|---|---|
| TypeScript errors | 0 |
| ESLint errors | 0 |
| ESLint warnings | 7 (all exhaustive-deps) |
| `any` type occurrences in source | 0 |
| Dark mode violations | 0 |
| Unprotected data routes | 0 (2 intentional public/webhook routes excluded) |
| Client-side service-role key leaks | 0 |
| Un-scoped `organization_id` queries | 0 |
| Phantom table references in main code | 0 |
| Current prod deployment state | READY |
| Production URL status | 200 OK |

---

**Run completed:** 2026-04-11
**Commit at time of run:** `0dc0f32` (main)
**Overall status:** ✅ **HEALTHY** — no critical or warning-level issues requiring fixes.
