# EntitleFlow QA & Code Health Report

**Date:** 2026-04-18
**Scope:** Scheduled automated sweep — static analysis, code quality, deployment, schema drift
**Verdict:** ✅ **ALL GREEN** — no blocking issues, no fixes required

---

## Summary

Every check in the 5-phase sweep passed. The codebase is in exceptional shape heading into the Q2 product-loop work. One forward-looking deprecation warning from Next.js 16 and seven non-blocking react-hooks lint warnings are the only items worth noting, and none of them required an auto-fix commit.

| Phase | Status | Notes |
|---|---|---|
| 1. Static Analysis (typecheck / lint / build) | ✅ PASS | 0 TS errors, 0 lint errors, build successful |
| 2. Code Quality Audit | ✅ PASS | 0 dark-mode classes, 0 `: any`, 0 service-key leaks, 0 bad table refs |
| 3. Deployment Verification (Vercel) | ✅ PASS | Latest deploy READY, site HTTP 200 |
| 4. Database Schema Drift (Supabase) | ✅ PASS | 35/35 tables in sync with `lib/database.types.ts` |
| 5. Report | ✅ This file | — |

---

## Phase 1 — Static Analysis

- **`npm run typecheck`** → exit 0. Full uncached run (after deleting `tsconfig.tsbuildinfo`) completed in **6.86s** with zero errors under `strict: true`.
- **`npm run lint`** → exit 0. Zero errors. Seven `react-hooks/exhaustive-deps` warnings (detailed below).
- **`npm run build`** → succeeded. 111 static/dynamic pages compiled. One deprecation warning surfaced:

> ⚠️ **Next.js 16 deprecation:** the `middleware` file convention is deprecated. Next.js wants this renamed to `proxy` in a future major. Current file: `middleware.ts` at repo root (thin wrapper around `lib/supabase/middleware.ts`). **Not blocking today** but should be addressed before upgrading to Next 17.

### Lint warnings (react-hooks/exhaustive-deps) — 7 total

| File | Line | Note |
|---|---|---|
| `app/app/documents/page.tsx` | 326 | effect deps stale |
| `app/app/notifications/page.tsx` | 284 | effect deps stale |
| `app/app/projects/map/page.tsx` | 46 | effect deps stale |
| `app/app/settings/team/page.tsx` | 150 | effect deps stale |
| `components/app/document-parse-status.tsx` | 108, 149, 167 | three stale-deps warnings on polling effects |

These are informational. None of them are causing observable bugs — but they are cheap to fix and worth knocking out during the next cleanup pass.

---

## Phase 2 — Code Quality Audit

| Check | Result |
|---|---|
| Tailwind dark-mode classes (`dark:(bg\|text\|border\|ring\|shadow\|from\|to\|via\|hover\|focus\|placeholder\|divide)-`) | **0 matches** |
| `: any` explicit type annotations | **0** (all `as any` casts are narrow, commented, and justified) |
| `SUPABASE_SERVICE_ROLE_KEY` referenced in client code | **0 matches** |
| `NEXT_PUBLIC_*_SERVICE_ROLE` misnamed env vars | **0 matches** |
| API routes touching permit/project/document data without `supabase.auth.getUser()` | **0 unprotected** (2 intentional exceptions documented below) |
| `.from('<table>')` references to tables that don't exist in live DB | **0** |
| Missing `organization_id` scoping on cross-org queries | **0 confirmed issues** (2 false positives inspected & cleared) |

### Intentionally public API routes (not a bug)

- `app/api/leads/route.ts` — marketing lead capture. Must be open to anonymous form submissions. Validated via Zod schema, writes only to `marketing_leads`.
- `app/api/email/inbound/route.ts` — inbound email webhook from Apps Script. Authenticated via shared secret (`INBOUND_EMAIL_SECRET` header or query param), not session cookie. Appropriate pattern for webhooks.

### False positives on org-scoping scanner (manually cleared)

- `app/api/company-admin/groups/members/route.ts` — scoping happens via JS-level check (`group.organization_id !== admin.organization_id`) after the fetch rather than a chained `.eq()`. Functionally equivalent; safe.
- `app/api/company-admin/users/[id]/reset-password/route.ts` — same pattern (`user.organization_id !== admin.organization_id`). Safe.

### False positive on missing-table scanner (manually cleared)

- `supabase.storage.from('avatars')` in `app/app/settings/profile/page.tsx` (lines 149, 154) — that's the Supabase **Storage bucket** API, not a Postgres table. Bucket exists and is in use.

---

## Phase 3 — Deployment Verification

- **Project:** `prj_IiB5CH8l7YJSAIiD4qJ86Rc34PdL` (team `team_BZLzhz1aQpxLSF2bW2bL9GSh`)
- **Latest deployment:** `dpl_Er4x6jaHVYRs6NzeWEtG5rT4sFCz`
  - Commit: `0dc0f32` — *"fix: profile avatar not rendering in portal topbar"*
  - State: **READY**
  - Build logs: clean, no errors
- **Production URL:** `https://entitleflow.com`
  - HTTP status: **200**
  - Response size: 93,089 bytes
  - TTFB: 284ms

No failed deployments in recent history. No runtime errors in the most recent deployment's logs.

---

## Phase 4 — Database Schema Drift

Cross-referenced every table in `lib/database.types.ts` against the live Supabase project (`sjyzqqratghsjttqeghy`) via `list_tables`.

- **Tables in `database.types.ts`:** 35
- **Tables in live DB (public schema):** 35
- **Drift:** **0 tables**

Every `.from('<table>')` call in the codebase resolves to a real table (or, in the case of `avatars`, a real Storage bucket). No orphan types. No missing tables.

---

## Fixes Applied

**None.** No auto-fixable issues were found. No commits made. No pushes.

---

## Recommendations (non-urgent)

1. **Rename `middleware.ts` → `proxy.ts`** before the next Next.js major. Low-risk, 2-minute change. The deprecation warning will keep shouting on every build until this is done.
2. **Clean up the 7 `react-hooks/exhaustive-deps` warnings.** None are causing observable bugs, but `components/app/document-parse-status.tsx` has three in one file — worth a pass when you're next in that component. Usually a `useCallback` or adding the missing dep is enough.
3. **Incremental build cache behaved correctly.** The suspicious 2.17s first-run was stale `tsconfig.tsbuildinfo` — fresh full-build took 6.86s. No action needed, but worth knowing that deleting `.tsbuildinfo` before a deploy-verification run gives a more honest signal.

---

## Metrics Snapshot

```
typecheck_errors:           0
lint_errors:                0
lint_warnings:              7   (all react-hooks/exhaustive-deps)
build_status:               success
explicit_any_types:         0
dark_mode_classes:          0
client_service_key_leaks:   0
unprotected_sensitive_apis: 0   (2 intentionally-public routes documented)
bad_table_references:       0
schema_drift:               0   (35/35 tables in sync)
vercel_deployment_state:    READY
production_http_status:     200
production_ttfb_ms:         284
```

---

*Automated by scheduled QA sweep. Next run: 2026-04-25.*
