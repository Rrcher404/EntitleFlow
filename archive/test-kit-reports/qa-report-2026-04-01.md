# EntitleFlow QA Report — 2026-04-01

**Run type:** Scheduled autonomous sweep
**Model:** Claude Opus 4.6
**Commit at time of scan:** `0dc0f32` (fix: profile avatar not rendering in portal topbar)

---

## Summary

| Check | Status | Details |
|---|---|---|
| TypeScript typecheck | **PASS** | 0 errors |
| ESLint | **PASS** | 0 errors, 7 warnings (all `react-hooks/exhaustive-deps`) |
| Production build | **SKIP** | FUSE filesystem error in sandbox — not a code issue (see note) |
| Latest deployment | **PASS** | `dpl_Er4x6jaHVYRs6NzeWEtG5rT4sFCz` — READY (production) |
| Dark mode violations | **PASS** | 0 `dark:` Tailwind classes found |
| Service role key exposure | **PASS** | No `SUPABASE_SERVICE_ROLE_KEY` in client files |
| Auth on API routes | **PASS** | All data routes authenticated (see details) |
| Incorrect table references | **PASS** | No references to non-existent tables |
| Database schema drift | **PASS** | 35/35 tables match between `database.types.ts` and live Supabase |
| `any` type usage | **PASS** | 0 occurrences in main codebase |

**Overall: HEALTHY — no critical or warning issues found.**

---

## Phase 1: Static Analysis

### TypeScript (`tsc --noEmit`)
**Result: PASS** — Clean run, zero errors.

### ESLint (`npm run lint`)
**Result: PASS (7 warnings, 0 errors)**

All 7 warnings are `react-hooks/exhaustive-deps` — missing dependencies in `useCallback` / `useEffect` arrays:

1. `app/app/documents/page.tsx:326` — missing `supabase`
2. `app/app/notifications/page.tsx:284` — missing `supabase`
3. `app/app/projects/map/page.tsx:46` — missing `loadProjects`
4. `app/app/settings/team/page.tsx:150` — missing `loadTeamData`
5. `components/app/document-parse-status.tsx:108` — missing `onParseComplete`, `statusInfo.parse_status`
6. `components/app/document-parse-status.tsx:149` — missing `fetchStatus`
7. `components/app/document-parse-status.tsx:167` — missing `fetchStatus`

**Severity: Info** — These are intentional stable-reference patterns (adding these deps would cause infinite re-renders). No action needed unless behavior bugs surface.

### Production Build
**Result: SKIP** — Build failed with `EPERM: operation not permitted, unlink .next/.fuse_hidden*` which is a FUSE filesystem limitation in the sandbox environment, not a code defect. The latest Vercel deployment (from the same commit) built successfully, confirming the code is buildable.

---

## Phase 2: Code Quality Audit

### Dark Mode Violations
**Result: PASS — 0 violations**

The `dark:` keyword appears in 2 files but neither uses Tailwind `dark:` variant classes:
- `components/site/Logo.tsx:27` — Object key `dark: { ... }` for logo color variants (not a Tailwind class)
- `components/ui/onboarding-dialog.tsx` — Object property `dark: string` for step color config (not a Tailwind class)

These are data structures, not CSS classes. No violations.

### Auth Check on API Routes
**Result: PASS — All data routes are authenticated**

| Route Group | Auth Method | Count |
|---|---|---|
| `/api/comments/*` | `supabase.auth.getUser()` | 6 routes |
| `/api/tasks/` | `supabase.auth.getUser()` | 1 route |
| `/api/documents/*` | `supabase.auth.getUser()` | 5 routes |
| `/api/ai/*` | `supabase.auth.getUser()` | 10 routes |
| `/api/team/*` | `supabase.auth.getUser()` | 5 routes |
| `/api/notifications/*` | `supabase.auth.getUser()` | 2 routes |
| `/api/email/send` | `supabase.auth.getUser()` | 1 route |
| `/api/geocode/` | `supabase.auth.getUser()` | 1 route |
| `/api/admin/*` | `verifyAdmin()` | 14 routes |
| `/api/company-admin/*` | `verifyCompanyAdmin()` | 10 routes |

**Intentionally unauthenticated routes (correct behavior):**
- `/api/leads/` — Public lead capture form submission (no user data exposed)
- `/api/email/inbound/` — Webhook endpoint using admin client with its own authorization logic

### Service Role Key Exposure
**Result: PASS** — `SUPABASE_SERVICE_ROLE_KEY` appears only in server-side files: `lib/supabase/server.ts`, `lib/admin/auth.ts`, and `app/api/email/inbound/route.ts`. No `'use client'` files reference it.

### Incorrect Table References
**Result: PASS** — Zero references to known-wrong table names (`audit_logs`, `organization_members`, `organization_licenses`, `password_reset_tokens`, `licenses` standalone, `user_licenses`, `license_assignment_log`) in the main codebase. These were all cleaned up in the March 2026 stabilization sprints.

### `any` Type Usage
**Result: PASS — 0 occurrences in main codebase**

The massive lint cleanup on 2026-03-25 (`a8841d0`) removed all 196 vestigial `as any` casts and `: any` types. The main codebase is now fully typed. Note: worktree copies (`.claude/worktrees/`) still contain the old `any` types but these are not part of the deployed codebase.

### Organization ID Filtering (company-admin routes)
**Result: PASS** — All company-admin routes use `verifyCompanyAdmin()` which returns the admin's `organization_id`, and subsequent queries filter by it. Spot-checked: users, groups, permissions, license-requests, security, audit, and storage routes all scope to org.

---

## Phase 3: Deployment Verification

**Latest deployment:** `dpl_Er4x6jaHVYRs6NzeWEtG5rT4sFCz`
- **State:** READY
- **Target:** production
- **Created:** 2026-03-25T18:38:09Z
- **Commit:** `0dc0f32` — "fix: profile avatar not rendering in portal topbar"
- **Bundler:** Turbopack
- **Inspector:** [View on Vercel](https://vercel.com/entitleflow-nc/entitle-flow/Er4x6jaHVYRs6NzeWEtG5rT4sFCz)

**Deployment history (last 20):** 15 READY, 5 ERROR. All errors are historical (pre-March 25) and were resolved in subsequent commits. The last 9 consecutive deployments are all READY — strong stability trend.

---

## Phase 4: Database Schema Drift

**Result: PASS — Perfect alignment**

All 35 tables in `lib/database.types.ts` exist in the live Supabase database, and all 35 live tables are represented in the types file. No drift detected.

| Tables in types file | Tables in live DB | Match |
|---|---|---|
| 35 | 35 | 100% |

All tables have RLS enabled.

---

## Metrics

| Metric | Value | Trend vs 2026-03-31 |
|---|---|---|
| TypeScript errors | 0 | Stable |
| ESLint errors | 0 | Stable |
| ESLint warnings | 7 | Stable |
| `any` type occurrences | 0 | Stable |
| Dark mode violations | 0 | Stable |
| Unprotected data routes | 0 | Stable |
| Wrong table references | 0 | Stable |
| DB schema drift | 0 tables | Stable |
| Consecutive successful deploys | 9 | +1 |

---

## Fixes Applied

None — no issues requiring auto-fix were found.

---

## Recommendations

1. **ESLint warnings (Info):** The 7 `exhaustive-deps` warnings in `documents/page.tsx`, `notifications/page.tsx`, `projects/map/page.tsx`, `settings/team/page.tsx`, and `document-parse-status.tsx` are stable and intentional, but consider adding `// eslint-disable-next-line react-hooks/exhaustive-deps` comments with rationale to suppress them cleanly.

2. **Worktree cleanup:** Three stale worktrees exist under `.claude/worktrees/` (zealous-wozniak, priceless-kapitsa, pedantic-brattain). These contain old `any` types and outdated code. Consider cleaning them up to reduce grep noise and disk usage: `git worktree remove .claude/worktrees/<name>`.

3. **Build verification gap:** The sandbox FUSE limitation prevents running `npm run build` in this environment. Production builds are verified through Vercel, but consider adding a GitHub Actions CI workflow to run typecheck + lint + build on every push for defense-in-depth.

---

*Report generated autonomously by scheduled QA agent.*
