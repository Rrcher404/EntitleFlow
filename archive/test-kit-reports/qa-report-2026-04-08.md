# EntitleFlow QA Report — 2026-04-08

**Run type:** Automated scheduled sweep
**Branch:** main
**Latest commit:** `0dc0f3e` — fix: profile avatar not rendering in portal topbar

---

## Summary

| Check | Status | Details |
|---|---|---|
| TypeScript typecheck | **PASS** | `tsc --noEmit` — 0 errors |
| ESLint | **PASS** | 0 errors, 7 warnings (all `react-hooks/exhaustive-deps`) |
| Production build | **SKIP** | Cannot run in sandbox (FUSE filesystem `EPERM` on `.next/` cache) — Vercel build is the authority |
| Vercel deployment | **PASS** | Latest production deployment `dpl_Er4x6jaHV...` is READY |
| Database schema drift | **PASS** | All 34 code-referenced tables exist in live Supabase |

---

## Phase 1: Static Analysis

### TypeScript Typecheck
**Result: PASS** — `tsc --noEmit` completed with zero errors.

### ESLint
**Result: PASS (7 warnings, 0 errors)**

Warnings (all `react-hooks/exhaustive-deps`):
1. `app/app/documents/page.tsx:326` — missing dep `supabase` in useCallback
2. `app/app/notifications/page.tsx:284` — missing dep `supabase` in useCallback
3. `app/app/projects/map/page.tsx:46` — missing dep `loadProjects` in useEffect
4. `app/app/settings/team/page.tsx:150` — missing dep `loadTeamData` in useEffect
5. `components/app/document-parse-status.tsx:108` — missing deps `onParseComplete`, `statusInfo.parse_status`
6. `components/app/document-parse-status.tsx:149` — missing dep `fetchStatus`
7. `components/app/document-parse-status.tsx:167` — missing dep `fetchStatus`

**Note:** These are intentional dependency omissions to prevent infinite re-render loops. No action required.

### Production Build
**Result: SKIP** — The sandbox environment cannot clean the `.next/` cache due to FUSE filesystem restrictions. Vercel production build (latest deployment) succeeded, which is the authoritative build check.

---

## Phase 2: Code Quality Audit

### Dark Mode Violations
**Result: PASS — No Tailwind `dark:` class violations found.**

The `dark:` string appears in 2 files but these are **not** Tailwind dark mode classes:
- `components/site/Logo.tsx:27` — Object property `dark: { ... }` for SVG color variants (logo theming data, not CSS)
- `components/ui/onboarding-dialog.tsx` — Object property `dark: string` for step color data (hex values for UI accents, not CSS)

No `dark:` Tailwind utility classes exist in the codebase.

### API Route Authentication
**Result: PASS — All API routes are properly protected.**

| Route Group | Auth Method | Status |
|---|---|---|
| `/api/leads/` | Intentionally public (marketing form) | OK |
| `/api/comments/*` | `createServerSupabaseClient()` + `getUser()` | OK |
| `/api/tasks/` | `createServerSupabaseClient()` + `getUser()` | OK |
| `/api/team/*` | `createServerSupabaseClient()` + `getUser()` | OK |
| `/api/ai/*` (9 routes) | `createServerSupabaseClient()` + `getUser()` | OK |
| `/api/documents/*` | `createServerSupabaseClient()` + `getUser()` | OK |
| `/api/notifications/*` | `createServerSupabaseClient()` + `getUser()` | OK |
| `/api/email/*` | `createServerSupabaseClient()` + `getUser()` | OK |
| `/api/geocode/` | `createServerSupabaseClient()` + `getUser()` | OK |
| `/api/admin/*` (12 routes) | `verifyAdmin()` → `getUser()` + `is_super_admin` check | OK |
| `/api/company-admin/*` (12 routes) | `verifyCompanyAdmin()` + org scoping | OK |

### Service Role Key Exposure
**Result: PASS** — `SUPABASE_SERVICE_ROLE_KEY` appears in code only in server-side files:
- `lib/supabase/server.ts` — `getSupabaseAdminClient()`
- `lib/admin/auth.ts` — `verifyAdmin()`
- `app/api/email/inbound/route.ts` — server-only API route

No `'use client'` files reference the service role key.

### Incorrect Table References
**Result: PASS** — No references to known-wrong table names (`audit_logs`, `organization_members`, `organization_licenses`, `password_reset_tokens`, standalone `licenses`, `user_licenses`, `license_assignment_log`) found in `app/api/` routes.

**Note:** These wrong table names DO appear in `.claude/worktrees/` directories (stale worktree copies). The main codebase is clean.

### Organization ID Filtering (Company Admin Routes)
**Result: PASS** — All 12 `/api/company-admin/` routes include `organization_id` filtering.

### `any` Type Usage
**Result: PASS** — 0 occurrences of `: any` in the main codebase (`app/`, `lib/`, `components/`). The recent lint cleanup commit (`a8841d0`) eliminated all `any` types.

---

## Phase 3: Deployment Verification

**Latest Production Deployment:**
- ID: `dpl_Er4x6jaHVYRs6NzeWEtG5rT4sFCz`
- State: **READY**
- Commit: `0dc0f32` — fix: profile avatar not rendering in portal topbar
- Created: 2026-04-07
- URL: entitle-flow-j0pi6arsm-entitleflow-nc.vercel.app

**Deployment History (last 20):**
- 14 READY deployments
- 6 ERROR deployments (all historical, already resolved by subsequent commits)

No action needed — production is healthy.

---

## Phase 4: Database Schema Drift

**Live Supabase tables (35 total):**
marketing_leads, organizations, profiles, projects, permits, permit_status_history, comments, documents, activity_log, deadlines, jurisdictions, platform_config, feature_flags, announcements, admin_audit_log, team_members, team_invitations, comment_assignments, notifications, notification_preferences, parse_jobs, email_queue, flowe_conversations, flowe_messages, flowe_knowledge, license_definitions, role_permissions, user_permission_overrides, password_reset_config, user_activity_tracking, company_groups, company_group_members, license_change_requests, organization_contracts, contract_change_usage

**Code references 34 tables via `.from()` calls.** All match live database tables.

| Drift Check | Status |
|---|---|
| Tables in code but not in DB | **None** (`avatars` is a Storage bucket, not a table) |
| Tables in DB but not referenced in code | `email_queue`, `contract_change_usage` — exist but unused currently |

---

## Fixes Applied

**None required.** All checks passed.

---

## Recommendations

### Info

1. **Stale worktrees:** The `.claude/worktrees/` directory contains 3 stale worktrees (`zealous-wozniak`, `priceless-kapitsa`, `pedantic-brattain`) that reference outdated code including wrong table names like `organization_members`. Consider cleaning these up with `git worktree prune`.

2. **ESLint warnings:** The 7 `react-hooks/exhaustive-deps` warnings are intentional (preventing infinite loops), but consider adding `// eslint-disable-next-line react-hooks/exhaustive-deps` comments to silence them explicitly and document the intent.

3. **Unused DB tables:** `email_queue` and `contract_change_usage` exist in the database but aren't referenced in application code yet. These are likely reserved for upcoming Q2 features (email ingestion pipeline and contract tracking).

4. **`.eslintignore` deprecation:** ESLint is emitting a warning about `.eslintignore` being deprecated in favor of `ignores` in `eslint.config.js`. Consider migrating.

---

## Metrics

| Metric | Value |
|---|---|
| TypeScript errors | 0 |
| ESLint errors | 0 |
| ESLint warnings | 7 |
| `any` type occurrences | 0 |
| Dark mode violations | 0 |
| Unprotected API routes | 0 |
| Missing org-id filters | 0 |
| Service role key in client code | 0 |
| Schema drift (tables) | 0 |
| Production deployment state | READY |
