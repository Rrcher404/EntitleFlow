# EntitleFlow QA Report — 2026-04-21

Autonomous QA sweep by scheduled task `entitleflow-deep-test`.
Branch: `main` @ `0dc0f32` (no uncommitted changes beyond `next-env.d.ts` regeneration + untracked docs and prior QA reports).

## Summary

| Check | Status |
|---|---|
| TypeScript (`tsc --noEmit`) | ✅ PASS — 0 errors |
| ESLint (`npm run lint`) | ✅ PASS — 0 errors, 7 warnings |
| Production build (`npm run build`) | ✅ PASS — 111 static pages, 6.1s compile, 17.29s total |
| Latest Vercel deployment | ✅ READY (`dpl_Er4x6jaHVYRs6NzeWEtG5rT4sFCz`) |
| Production URL (`entitleflow.com`) | ✅ Reachable |
| DB schema drift | ✅ No code-to-schema mismatches |

**Verdict:** Clean sweep. No autonomous fixes applied — nothing to fix.

## Phase 1 — Static Analysis

### TypeScript
`./node_modules/.bin/tsc --noEmit` → exit 0. Zero type errors.

### ESLint
`npm run lint` → exit 0. 7 warnings, all `react-hooks/exhaustive-deps`:

- `app/app/documents/page.tsx:326` — `useCallback` missing `supabase` dep
- `app/app/notifications/page.tsx:284` — `useCallback` missing `supabase` dep
- `app/app/projects/map/page.tsx:46` — `useEffect` missing `loadProjects` dep
- `app/app/settings/team/page.tsx:150` — `useEffect` missing `loadTeamData` dep
- `components/app/document-parse-status.tsx:108,149,167` — 3 dep-array warnings

These are stable cross-render references (the `supabase` client from `createClient()` is stable, and the load functions are defined at component scope). Inlining them into deps would cause unnecessary re-renders. Low-priority — flag-only, no fix applied.

Also: a config deprecation notice — `.eslintignore` is deprecated in ESLint v9; should migrate to `ignores` in `eslint.config.js`. Tracked as a recommendation below.

### Production build
`npm run build` → exit 0. Turbopack compiled in 6.1s; all 111 static pages generated successfully. Nonblocking deprecation warning: `middleware` file convention is deprecated in Next.js 16 — should rename to `proxy` per the migration notice at `nextjs.org/docs/messages/middleware-to-proxy`.

## Phase 2 — Code Quality Audit

All scans scoped to the canonical tree. `.claude/worktrees/*` (three stale worktree backups: `zealous-wozniak`, `pedantic-brattain`, `priceless-kapitsa`) were excluded — they are snapshots, not shipped code.

### Dark-mode violations
**0 actual violations.** Grep initially surfaced 6 occurrences across `components/ui/onboarding-dialog.tsx` and `components/site/Logo.tsx`, but all are object-property keys (e.g., `colors: { dark: '#0f3c35', light: '#e8f5f2' }`), not Tailwind `dark:` variants. Light-mode-only invariant is intact.

### Service role key exposure
**0 violations.** `SUPABASE_SERVICE_ROLE_KEY` appears only in server code (`lib/supabase/server.ts`, `lib/admin/auth.ts`, `app/api/email/inbound/route.ts`) and documentation. No `'use client'` file references it.

### Incorrect table references
**0 violations.** None of the known-wrong table names (`audit_logs`, `organization_members`, `organization_licenses`, `password_reset_tokens`, `licenses`, `user_licenses`, `license_assignment_log`) are referenced in the main tree. Residual occurrences live in `.claude/worktrees/*` and documentation (expected).

### `: any` type usage
**0 occurrences** in main tree `.ts`/`.tsx` (excluding `database.types.ts`). The April 18 lint sprint (`a8841d0`) removed all 196 vestigial `as any` casts and enforces the `Database` generic end-to-end.

### API route authentication
**All 57 API routes are authenticated.** Initial scan flagged 13 admin routes missing `getUser`/`verifyCompanyAdmin`; follow-up confirmed they all use `verifyAdmin()` from `lib/admin/auth.ts` (a super-admin-only helper that calls `getUser()` internally and additionally checks `is_super_admin`). Auth-helper matrix:

- `/api/admin/*` → `verifyAdmin()` (super-admin only)
- `/api/company-admin/*` → `verifyCompanyAdmin()` (org-scoped admin)
- `/api/ai/*`, `/api/comments/*`, `/api/documents/*`, `/api/projects/*`, `/api/permits/*`, `/api/notifications/*`, `/api/tasks/*` → `supabase.auth.getUser()`
- Intentionally unauthenticated: `/api/leads` (public marketing form), `/api/team/accept` (token-gated invite), `/api/email/inbound` (shared-secret webhook)

### Organization-scoping on company-admin routes
**All company-admin queries are org-scoped.** Every route under `app/api/company-admin/` that queries `projects`, `permits`, `documents`, or `profiles` includes an `organization_id` filter.

## Phase 3 — Deployment Verification

**Latest production deployment:** `dpl_Er4x6jaHVYRs6NzeWEtG5rT4sFCz` @ commit `0dc0f32` (*"fix: profile avatar not rendering in portal topbar"*) — state `READY`.

Recent history is healthy: the last 10 production deployments are all `READY`. Four `ERROR` deployments appear earlier in the tail (2026-03-20 → 2026-03-21 range), all from the RBAC migration / `@ts-nocheck` removal sprint, and all superseded by a successful deploy within 15 minutes.

**entitleflow.com** responded with a full 200 OK page body (~100kB) — production URL is live.

## Phase 4 — Database Schema Drift

Supabase project `sjyzqqratghsjttqeghy` has **35 tables** in the `public` schema with RLS enabled on every one. Code-referenced tables all exist in the live DB. Specifically verified present:

- `team_members` (replaces legacy `organization_members`)
- `admin_audit_log` (replaces legacy `audit_logs`)
- `license_definitions`, `role_permissions`, `user_permission_overrides`
- `password_reset_config` (replaces legacy `password_reset_tokens`)
- `license_change_requests`, `organization_contracts`, `contract_change_usage`

No drift. No orphaned code references.

## Fixes Applied

None. Nothing warranted autonomous changes.

## Recommendations (Human Attention)

These are non-blocking but worth scheduling:

1. **Migrate `.eslintignore` → `ignores` in `eslint.config.js`.** ESLint v9 deprecated the ignore file; currently a warning, will become an error.
2. **Rename `middleware.ts` → `proxy.ts`.** Next.js 16 deprecated the `middleware` convention. Migration guide: `nextjs.org/docs/messages/middleware-to-proxy`.
3. **Address the 7 `exhaustive-deps` warnings** — either by memoizing the load functions with `useCallback` and adding them to deps, or suppressing with eslint-disable comments where the closure is provably stable.
4. **Prune stale worktrees.** `.claude/worktrees/{zealous-wozniak, pedantic-brattain, priceless-kapitsa}` contain outdated copies that pollute every grep/scan. Safe to delete once any work in them is merged.
5. **Untracked files in working tree** — prior QA reports, `ADR-001-GRANTFLOW-ARCHITECTURE.md`, `EntitleFlow-Investor-Mentor-Briefing.md`, and several analysis HTMLs are uncommitted. Decide whether to `.gitignore` them or commit under `docs/`.

## Metrics

| Metric | Value |
|---|---|
| `: any` occurrences (main tree) | 0 |
| Dark-mode Tailwind violations | 0 |
| Unprotected API routes | 0 |
| Routes missing org-scoping (company-admin) | 0 |
| Wrong-table-name references | 0 |
| ESLint errors / warnings | 0 / 7 |
| TypeScript errors | 0 |
| Static pages generated | 111 |
| DB tables (public schema) | 35 |
| Last 10 production deploys | 10 READY / 0 ERROR |

---
*Generated by automated QA sweep — scheduled task `entitleflow-deep-test`.*
